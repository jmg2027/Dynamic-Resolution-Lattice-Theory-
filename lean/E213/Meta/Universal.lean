import E213.Meta.Universal.LensClaim
import E213.Meta.Universal.MorphismFactor
import E213.Meta.Universal.Reflection

/-! Spec-as-code entry point for `E213.Meta.Universal`.

  Universal-Lens metatheorems: every framework is a Lens.

  ## Files

    * `LensClaim`     — formal "every framework is a Lens" claim
    * `MorphismFactor` — image-Lens factorisation of a universal
                         morphism
    * `Reflection`    — Lens reflection of `universalMorphism`
-/
