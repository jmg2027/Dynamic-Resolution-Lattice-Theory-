import E213.Math.Tactic.IntSquare

/-! Spec-as-code entry point for `E213.Math.Tactic` — 213-native
  tactics.  Currently only IntSquare builds clean; HurwitzRing
  + QuadExtension depend on CayleyDickson types whose namespace
  refactor is in progress (see CayleyDickson.lean exclusions). -/
