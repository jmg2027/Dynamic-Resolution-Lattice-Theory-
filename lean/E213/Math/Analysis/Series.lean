import E213.Math.Analysis.Series.CutGeomSeries
import E213.Math.Analysis.Series.CutSequence
import E213.Math.Analysis.Series.CutSeries

/-! Spec-as-code entry point for `E213.Math.Analysis.Series`.

  Series + sequences on cuts.

  ## Files

    * `CutSequence`     — sequence-of-cuts type + basic ops
    * `CutSeries`       — series partial-sum machinery
    * `CutGeomSeries`   — geometric series Σ rⁿ closed-form
                          + convergence
-/
