import E213.Math.Analysis.ClassicCalc.ClassicCalc
import E213.Math.Analysis.ClassicCalc.ClassicCalcCombinators
import E213.Math.Analysis.ClassicCalc.ClassicCalcMid

/-! Spec-as-code entry point for `E213.Math.Analysis.ClassicCalc`.

  Classical calculus structure on cuts.

  ## Files

    * `ClassicCalc`             — main classical-calculus
                                  derivation rules (linear,
                                  product, chain) on cuts
    * `ClassicCalcCombinators`  — higher-order combinators
                                  built on the rules above
    * `ClassicCalcMid`          — midpoint-localised variant
                                  (matches `DifferentiableMid`)
-/
