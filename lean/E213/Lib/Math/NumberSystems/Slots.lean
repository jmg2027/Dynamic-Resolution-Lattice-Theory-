import E213.Lib.Math.NumberSystems.Slots.Rat213
import E213.Lib.Math.NumberSystems.Slots.CompletionDichotomy
import E213.Lib.Math.NumberSystems.Slots.GaussTuple
import E213.Lib.Math.NumberSystems.Slots.PairPow

/-! # Slots — the slot-programme cluster

The signed lowest-terms normal form and derived order (`Rat213`), the
frame-indexed collapse-vs-rigid witnesses (`CompletionDichotomy`), the
4-axis tuple product (`GaussTuple`), and the pair-exponent fiber
transport (`PairPow`).  Narrative:
`theory/math/numbersystems/slot_arithmetic.md`; volume `book/slots/`.
Generic pair-layer infrastructure lives in `Meta/Nat/{PairOp,UnitList}`. -/
