import E213.Hypervisor.Leaves.DepthIncomparable
import E213.Hypervisor.Leaves.DepthJoin
import E213.Hypervisor.Leaves.Mod3
import E213.Hypervisor.Leaves.ModNat
import E213.Hypervisor.Leaves.RefinesParity

/-! Spec-as-code entry point for `E213.Hypervisor.Leaves`.

  Depth-leaf hierarchy: the bottom of the refines preorder.

  ## Files

    * `Mod3`              — mod-3 leaf
    * `ModNat`            — generic mod-N leaf family
    * `RefinesParity`     — parity-refinement witness
    * `DepthJoin`         — join of depth-comparable leaves
    * `DepthIncomparable` — depth-incomparable witness
-/
