import E213.Theory.Tower.NatPairToInt
import E213.Theory.Tower.NatPairToQPos
import E213.Theory.Tower.NatTripleToZ2

/-! Spec-as-code entry point for `E213.Theory.Tower`.

  Number-tower constructions on Closed Nat213 (Raw-derived):
  ℕ → ℤ via pairs, ℕ → ℚ⁺ via pairs, ℕ × ℕ × ℕ → ℤ/2 triples. -/
