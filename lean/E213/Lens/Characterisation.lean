import E213.Lens.Characterisation.Catalog
import E213.Lens.Characterisation.Core

/-! Spec-as-code entry point for `E213.Lens.Characterisation`.

  Lens characterisation predicates: which abstract conditions
  on a binary `combine` operation classify it as a Lens combine.

  ## Files

    * `Core`    — core characterisation typeclasses + helper lemmas
    * `Catalog` — concrete catalogue of binary-combine instances
                  matching the characterisation
-/
