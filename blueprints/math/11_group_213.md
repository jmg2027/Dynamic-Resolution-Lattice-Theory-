# Group Theory 213 — Blueprint

**Priority**: ★ (needed for DHA + physics tracks)

---

## 1. Why This Field

ZFC group theory:
- Group (G, ·, e, ⁻¹) axioms
- Subgroup, normal, quotient
- Group action, representation
- Lie group, simple group classification

Natural emergence in 213:
- **S₅** already formalized in DHA track (Lean 34+ theorems)
- **SU(5) GUT** already in standard-model track
- **Symmetric group** = permutation = atomic
- **Lie group** = continuous symmetry = Analysis 213 + linear

## 2. 213-native Emergence

### 2.1 Finite group from atomic

S₅ = permutation of 5 atoms.  |S₅| = 120 = d!  naturally appears.

ℤ/nℤ = atomic counting modulo n (Pigeonhole).

### 2.2 Group action on dyadic tree

Changing bisectN oracle = group action on trajectory space.
{alwaysTrue, alwaysFalse} = a kind of group element.

### 2.3 Representation

Linear algebra 213 (blueprint 09) + group → representation.
3×3 matrix representation of S₃, etc.

### 2.4 Lie algebra

Commutator of differential generators.  Analysis 213 + linear combined.
SU(5)'s 24 generators natural (already partially formalized).

## 3. Building Blocks

| Tool | Use |
|---|---|
| `Fin n`, `Equiv` | permutation |
| DHA `S5*.lean` | already in hand |
| standard-model `SU5Roots.lean` | Lie algebra |
| Linear 213 (blueprint 09) | matrix rep |

## 4. Phase Plan

### Phase GA — Group axioms (3-5 commits)

1. `Group G` structure (assoc, identity, inverse)
2. Subgroup, normal subgroup
3. Quotient G/N (using Lens)
4. Lagrange theorem skeleton

### Phase GB — Symmetric + cyclic

1. S_n via Equiv
2. ℤ/nℤ via Fin n
3. Cyclic group ⟨g⟩
4. Cayley theorem (G ⊂ S_|G|)

### Phase GC — Group action + orbit

1. Action on Set
2. Orbit, stabilizer
3. Burnside lemma (counting)
4. Sylow theorem (using atomic primes)

### Phase GD — Representation + Lie

1. Matrix rep (using linear 213)
2. Character
3. Lie algebra brackets
4. SU(5) 24 generators (standard-model integration)

### Phase GE — Capstone

First year undergraduate group theory + introductory representation theory.

## 5. Connections to Other Tracks

- **DHA**: S₅ representation theory (formalization in progress)
- **Standard Model**: SU(5), SU(3), SU(2), U(1)
- **Atoms**: orbital symmetry
- **Combinatorics 213**: counting via Burnside

## 6. Open Problems

- **Simple group classification** — massive theorem
- **Galois theory** — combine with number theory 213
- **Lie group full classification** — root system

## 7. Key Insights (★)

★ **S₅ = atomic group** — permutation of 213's d=5, natural.

★ **SU(5) = atomic GUT** — 25 channels = 5 ⊗ 5, natural.

★ **Galois extension is the core of Number Theory 213** — integrates up to RH.

## 8. First Marathon Command

```
"Start Phase GA.  Group axioms + S_5 via Equiv"
```

