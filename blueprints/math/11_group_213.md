# Group Theory 213 — Blueprint

**우선순위**: ★ (DHA + 물리 트랙 위해 필요)

---

## 1. 왜 이 분야인가

ZFC group theory:
- Group (G, ·, e, ⁻¹) axioms
- Subgroup, normal, quotient
- Group action, representation
- Lie group, simple group classification

213 의 자연 등장:
- **S₅** 이미 DHA 트랙 형식화 (Lean 34+ 정리)
- **SU(5) GUT** 이미 standard-model 트랙
- **Symmetric group** = permutation = atomic
- **Lie group** = continuous symmetry = 분석학 213 + linear

## 2. 213-native 등장

### 2.1 Finite group from atomic

S₅ = permutation of 5 atoms.  |S₅| = 120 = d!  자연 등장.

ℤ/nℤ = atomic counting modulo n (Pigeonhole).

### 2.2 Group action on dyadic tree

bisectN oracle 변경 = group action on trajectory space.
{alwaysTrue, alwaysFalse} = group element 의 일종.

### 2.3 Representation

Linear algebra 213 (blueprint 09) + group → representation.
3×3 matrix representation of S₃ 등.

### 2.4 Lie algebra

미분 generator 의 commutator.  분석학 213 + linear 결합.
SU(5) 의 24 generator 자연 (이미 형식화 일부).

## 3. 빌딩 블록

| 도구 | 활용 |
|---|---|
| `Fin n`, `Equiv` | permutation |
| DHA `S5*.lean` | 이미 보유 |
| standard-model `SU5Roots.lean` | Lie algebra |
| Linear 213 (blueprint 09) | matrix rep |

## 4. Phase 계획

### Phase GA — Group axioms (3-5 commits)

1. `Group G` structure (assoc, identity, inverse)
2. Subgroup, normal subgroup
3. Quotient G/N (Lens 활용)
4. Lagrange theorem skeleton

### Phase GB — Symmetric + cyclic

1. S_n via Equiv
2. ℤ/nℤ via Fin n
3. Cyclic group ⟨g⟩
4. Cayley theorem (G ⊂ S_|G|)

### Phase GC — Group action + orbit

1. Action on Set
2. Orbit, stabilizer
3. Burnside lemma (counting)
4. Sylow theorem (atomic prime 활용)

### Phase GD — Representation + Lie

1. Matrix rep (linear 213 활용)
2. Character
3. Lie algebra brackets
4. SU(5) 24 generator (standard-model 통합)

### Phase GE — Capstone

학부 군론 1년차 + 표현론 첫 단계.

## 5. 다른 트랙 연결

- **DHA**: S₅ 표현론 (이미 형식화 진행)
- **Standard Model**: SU(5), SU(3), SU(2), U(1)
- **Atoms**: orbital symmetry
- **Combinatorics 213**: counting via Burnside

## 6. 미해결 / Open

- **Simple group classification** — 거대 정리
- **Galois theory** — number theory 213 결합
- **Lie group full classification** — root system

## 7. 핵심 인사이트 (★)

★ **S₅ = atomic group** — 213 의 d=5 의 permutation, 자연.

★ **SU(5) = atomic GUT** — 25 channel = 5 ⊗ 5 자연.

★ **Galois extension 가 정수론 213 의 알맹이** — RH 까지 통합.

## 8. 첫 마라톤 명령

```
"Phase GA 시작.  Group axioms + S_5 via Equiv"
```

