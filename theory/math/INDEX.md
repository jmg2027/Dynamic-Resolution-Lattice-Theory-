# theory/math/

Math chapters.  Mirrors `lean/E213/Lib/Math/` (+ `Lens/Number/`).

## Closed chapters (57)

Hub chapters:
- [`cohomology/hodge_conjecture.md`](cohomology/hodge_conjecture.md) — HodgeConjecture/ (67 files, 6 layers)
- [`cayley_dickson/algebra_tower.md`](cayley_dickson/algebra_tower.md) — CayleyDickson/ (50 files)
- [`cayley_dickson/exceptional_axes.md`](cayley_dickson/exceptional_axes.md) — CayleyDickson/Tower/ E₆E₇E₈ derivation (13 files, ~65 PURE; `{√−3,√2,√5}` from `{NS,NT}` + the map `D(x)=x²−NT`; surd-free `ℕ` form)
- [`universe_chain.md`](universe_chain.md) — UniverseChain + Mobius213 + Nat213 (~32 files)
- [`real213.md`](real213.md) — Real213/ (57 files, 7 sub-clusters)
- [`dyadic_fsm.md`](dyadic_fsm.md) — DyadicFSM/ (101 files)
- [`signed_cut.md`](signed_cut.md) — SignedCut/ (35 files; absorbs G37/G38/G39)
- [`geometrization_conjecture.md`](geometrization_conjecture.md) — GeometrizationConjecture/ (13 files, R1 closed, R1+ partial)
- [`padic_real213.md`](padic_real213.md) — Padic/ (8 files, 308 PURE; ring axioms, Hensel inv/sqrt with existence + uniqueness, full ultrametric, Frobenius lift, Teichmüller Cauchy, ℚ_p, DRLT 5-adic anchor)

Cohomology sub-clusters (12 in `cohomology/`):
- `cohomology/{cochain, cup, cupaw, delta, bipartite, fractal, aurifeuillean, hodge, surfaces, universal, examples, bridge}.md`

Analysis sub-clusters (12 in `analysis/`):
- `analysis/{minimal_root, classic_calc, differentiation, flux_m_v_t, integration}.md`
- [`analysis/holonomic_modulus.md`](analysis/holonomic_modulus.md) — completeness as a *constructed* convergence rate: the general "rate-carrying ⟹ total modulus" generator (`RateModulus.rate_total_modulus`), the rate criterion `tailᵢ·k·dᵢ < 1`, and the `HolonomicReal` instances φ (`N=2k`) and e (`N=k+2`, `eHolonomicReal`).  Rate-carrying vs rate-free, not algebraic vs transcendental.  Frontier: π (rate-free, needs `μ(π)`), the depth-rank ⟶ rate bridge
- [`analysis/tower_native_completeness.md`](analysis/tower_native_completeness.md) — completability as a comparison of two growth-axes (cross-determinant `W` vs denominator `d`) *inside* the tower: the overtake boundary (`CrossDetOvertake`, constant `W` free / double-exp `W` breaks), Liouville tame on this axis (`LiouvilleModulus`, `W=d=c^{k!}` ⟹ free modulus), the finite-coordinate class closed under `×`/exponent (`DepthClosure`), the tower as a coordinate generator (`DepthCoordGenerator`), and the tower's top-lessness = the residue (`DepthCeilingResidue`).  Bundled in `TowerNativeCompleteness.tower_native_completeness_program`.  The `W = const` rung is universal: every real's continued-fraction convergent cross-determinant is a unit (`ContinuedFractionFloor.cf_det_sq`, `W² = 1`), and every real `≥ 1` completes through its continued fraction (`ContinuedFractionModulus.cf_universal_total_modulus`) — the algebraic-φ completion generalised to the whole real line
- [`analysis/refined_completability_engine.md`](analysis/refined_completability_engine.md) — refines the binary boundary into a two-axis engine: an **ordinal height** axis (`CompletabilityGrade`, completability `= ω·height + rate`, height an `ω`-coordinate over the exponential tower, height dominating rate) and an **intensional gauge** axis (`IntensionalCompletability`, the test antitone under rescaling/reduced canonical, the completion gauge-invariant).  Bundled in `RefinedCompletabilityEngine.refined_completability_engine`; the height tower has no top, its diagonal = the residue (`HeightTowerResidue`).  Completability = the lex ordinal `(height, rate)` of the gcd-reduced presentation, an intensional invariant of the cut
- [`analysis/spiral_coordinate_classification.md`](analysis/spiral_coordinate_classification.md) — a classification of reals finer than algebraic/transcendental and orthogonal to Mahler/Koksma/μ: two independent 213-native count-coordinates — **layer** (divergence depth, intensional, spectrum all of `ℕ`; e `3`, π `6`, but regular-CF collapses every real to depth 1 via `cf_det_sq`; `SpiralLayer`) and **axis** (arithmetic unit-group order, exactly `{2,4,6}` by Dirichlet+`φ(m)≤2`, realized as floor rotations: `ℤ` order 2, `ℤ[i]` order 4 `gaussian_floor_rotation`, `ℤ[ω]` order 6 `eisenstein_floor_rotation`) — plus the universal CF spiral and the residue (layer `∞`).  Gaussian 4-theorem (`ZIUnits.ZI_units_exact_four`) mirrors the Eisenstein 6-theorem.  **Axis exhaustiveness** closed ∅-axiom (`ImaginaryQuadraticUnitTrichotomy`: `a²+d·b²=1` forces `±1` for every `d≥2`, so `{2,4,6}` is a closed range, not three samples — the Dirichlet trichotomy made constructive, extended to all imaginary-quadratic *maximal* orders via `maximal_order_no_complex_unit`).  **Binary cover** (`axis_binary_cover`): `{2,4,6}=2·{1,2,3}`, midpoint `μᵏ=−1` (the Cassini sign = the "binary" of binary-polyhedral E₆E₇E₈).  Verified bridge to the crystallographic restriction (`Tower/SpiralAxisCrystallographic`: `{2,4,6}` = even half of `{1,2,3,4,6}`).  Bundled in `SpiralCoordinate.spiral_coordinate` (4 conjuncts).  Frontier: π's CF non-holonomicity (classical open)
- [`analysis/cf_holonomicity_hierarchy.md`](analysis/cf_holonomicity_hierarchy.md) — the **third spiral-layer reading**: the finite-difference depth of the *partial-quotient sequence* `(aᵢ)` (not the cross-determinant).  `QuasiPolyCF p a` = polynomial on each residue class mod `p` (the formal handle on "Hurwitzian / holonomic partial quotients").  Tiers: **0** periodic ⟹ quasi-poly (`periodic_quasipoly`, quadratic irrationals, Lagrange); **1** e=[2;1,2k,1] is `QuasiPolyCF 3` (`e_cf_quasipoly`) and tan 1=[1;1,1,3,1,5,…] is `QuasiPolyCF 2` (`tan_cf_quasipoly`) — the folklore "Hurwitzian ⟹ holonomic" made explicit; **top** non-Hurwitzian, inhabited by `2ⁿ` (`geometric_not_quasipoly`) which is C-finite hence holonomic, so `QuasiPolyCF ⊊ C-finite ⊊ holonomic` strictly.  Certificate `polyDepth d ⟹ Δ^{d+1}=0`.  `Cauchy/HurwitzianCF` 19 PURE.  Frontier: π non-holonomic (strictly above π non-Hurwitzian); FGS asymptotic route
- [`analysis/markov_spectrum.md`](analysis/markov_spectrum.md) — the Lagrange/Markov spectrum (the fine approximation constant `μ` discards) from the repo's own forms: the **golden form** `m²−mk−k²` (P-spiral invariant, disc 5) is the **first Markov form** — `golden_anisotropic` (Vieta descent) ⟹ `inf|Q|=1` ⟹ value `√5` = spectrum minimum (φ, worst-approximable, its Cassini `W=±1` floor = the form's minimum); silver form `x²−2y²` (disc 8) ⟹ `√8` (via `sqrt2_irrational`).  Vieta tree on `x²+y²+z²=NS·xyz` (`markov_vieta`, coeff `3=NS`): branch `(1,1,1)→…→(2,5,29)`.  `Real213/{GoldenFormMarkov,MarkovTree}` 13 PURE
- [`analysis/newton_gregory.md`](analysis/newton_gregory.md) — the faithful forward-difference calculus over `ℤ` (the readout group in which `Δ` closes under iteration): the **universal** identity `s(m+n)=Σ binom(n,j)(Δʲs)(m)` for every `s:ℕ→ℤ` (`newton_gregory`, `Eⁿ=(I+Δ)ⁿ`, no hypothesis), the matched **inverse** `Δⁿ=(E−I)ⁿ` (`newton_gregory_inverse`) bundling the binomial transform as an invertible change of basis (`binomial_transform_roundtrip`, `F∘G=id`), the **reconstruction** `polyDepthZ d s ⟹ s=Σ_{i≤d}(Δⁱs 0)·binom(·,i)` (`reconstruct`, **closes HANDOFF Open Problem #4** — the ℤ converse ℕ truncation could not state), and the **growth bound** `|s n|≤C·(n+1)^d`, `C=Σ|Δⁱs 0|` (`poly_bound`, **unblocks** the general `QuasiPolyCF ⟹ poly-bounded` ⟹ μ=2 bridge).  Obstruction pinned (`vObs=(n−2)(n−1)`: ℕ-`diff` clamps the first difference `−2→0`, a different Lens from ℤ-`diff`).  `Cauchy/NewtonGregory` 41 PURE.  **Application** (`Cauchy/QuasiPolyBound`, 14 PURE): `quasiPolyCFZ_poly_bounded` — quasi-polynomial continued fractions are polynomially bounded (reassemble per-residue `poly_bound` via pure finite-max + `n=p·⌊n/p⌋+n%p`), whence (cited) μ=2; witnesses periodic CFs (quadratic irrationals bounded, Lagrange) and e (`e_partial_quotients_poly_bounded`, subsuming `ePQ_linear_bound`).  **Ring structure** (`Cauchy/FiniteDepthAlgebra`, 22 PURE): finite-depth ℤ-sequences closed under `+`, scalar `·`, shift, and product with additive depths — `polyDepthZ_mul` (`polyDepthZ d s → polyDepthZ e t → polyDepthZ (d+e) (s·t)`) via the discrete Leibniz rule `diffZ_mul`, turning `DivergenceDepth`'s hand-counted depth arithmetic into a theorem.  **Involution** (`Cauchy/BinomialTransform`, 6 PURE): the sign-twisted transform `T s n = Σ_{j≤n}(−1)ʲbinom(n,j)s(j)` is self-inverse (`binomialT_involutive`, `T∘T=id`) and fixed-point-rich (`binomialT_fixed`, `s+Ts` always fixed) — settling the §5.2 self-reference question (Nat-style grounding, not Bool-style liar)

Discrete-substrate geometry (10):
- `modulus, number_grid, angle_structure, bipartite_decomp, cartesian_vs_disjoint, generation_rule, triangular_tower, operation_topology, level_topology, algebra213_meta_theorems`

Marathon-completed domains (6):
- `combinatorics, information, logic, probability, group, modular_arithmetic` (modular_arithmetic extended by G119 to 13 files: +Bezout / FLT / F_{p²})

Infrastructure (10):
- `axiom_systems, cross_domain_unification, pattern_catalog/pattern_catalog, linalg213, polynomial213, irrational, hyper, cauchy, topology, ode, multivariable, complex, measure, functional, extras, tactic, choice, geometry, cascade_calculus, modulus_structure`

Universal meta-structure (1):
- [`gra_book.md`](gra_book.md) — **GRA Book** (textbook treatment): Ch.0–9 + appendices. Formal Definition→Theorem→Proof flow. 7 axioms, 5 Readings (cohomology/operad/HoTT/graph/analysis), GRA Tower ↔ CD Tower duality, number system unification, dimensional proliferation fractal, Adelic decomposition, one-paragraph master statement. **CLOSED** (Marathon 16 — `lean/E213/Lib/Math/GRA/` umbrella, 28 files / 0 sorry). Lean cross-reference table in Appendix B.1.

Cross-frame synthesis (3):
- [`sym3_spine.md`](sym3_spine.md) — Sym(3) 8-fold decomposition `2·trivial ⊕ 3·standard` across K_{3,2}^{(c=2)} H¹, Thurston geometries, gluon octet, Akbulut cork; capstone `X1_sym3_cross_frame_capstone`
- [`mobius_canonical_equivalence.md`](mobius_canonical_equivalence.md) — Möbius P = [[2,1],[1,1]] as the canonical equivalence on cuts via Stern-Brocot mediant closure (7 files / 68 PURE); cutEq ↔ sternBrocotEq ∧ (0,0); ValidCutN + signedEq bridges; Pell unit invariant + atomicity anchor
- [`mobius213_p_orbit_closure.md`](mobius213_p_orbit_closure.md) — P-orbit naturalness boundary (13 Px modules / 206 PURE); tripartite K_{2,1,3} additive complement; mod-p periods + Lucas-Pell trace orbit; refined naturalness hierarchy
- [`phi_self_similarity.md`](phi_self_similarity.md) — φ as the single invariant of `P = [[2,1],[1,1]]`, read three ways (form / count `5^L` / limit-ratio φ), all ∀n ∅-axiom; Cassini norm `fib(2n+2)²+1 = fib(2n+2)·fib(2n+1)+fib(2n+1)²` ∀n; φ as a closed-form `ValidCut`; `fib_convergent_below_phi` ∀n.  Capstone `self_similarity_three_readings`
- [`completeness_relocated.md`](completeness_relocated.md) — completeness is **not** constitutive of a Real213 real (it is a leaf of the import graph, not a root); a real is a decision procedure against ℚ; completeness re-enters only as a limit operation on sequences (`AbCutSeq`), **unconditional** for every rate-carrying real — algebraic (φ: closed-form `N=2k`, `phiCompletion_limit_eq_phiCut`) and holonomic transcendental (e: total `N=k+2`, `ExpLog/EulerModulus.eHolonomicReal`, from the factorial rate) — and **modulus-gated** only for a rate-free presentation (π ∈ (14/5,4) pending its Wallis rate; the rate-free closure is the LEM `MonotonicBounded` §180–194 refuses).  The rate-carrying/rate-free split as a theorem
- [`probe_twist_conic.md`](probe_twist_conic.md) — the probe-twist (Möbius `P=[[2,1],[1,1]] ∈ SL₂(ℤ)`, hyperbolic) conserves the φ-norm `Q=m²−mk−k²`, so every cut wobbles along its own hyperbola `Q=N` (`ProbeTwistConic.Q_preserved`); the wobble step is `f⁻¹` (`ProbeTwistDynamics`).  What sorts the reals: quadratic irrationals ride **one** conic (φ/√2 formalised, general case = Lagrange's thm) — transcendentals **escape every** conic (e vs π differ only in escape-regularity).  Marks three tiers: Lean theorem / classical fact / out-of-scope (the elliptic-modular *production* of transcendentals is a separate higher theory this does not reach)
- [`completeness_without_completeness.md`](completeness_without_completeness.md) — **capstone paper**: the whole arc as one thesis ("completeness is a relocated finite operation"). Five parts — cut/modulus → probe-twist conic → cross-determinant divergence depth (1/3/6/∞, = P-recursive rank, ⊥ irrationality measure) → ordinal axis tower (`ratioLift`, `(h,d)<ω²`, exponent recursion, ε₀-is-not-the-end) → naming-the-ceiling-raising = the foundational residue (`diag_not_in_seq`, `cantor_general`, `self_covering_closure`). 15 sections, every `(L)` claim indexed to its ∅-axiom Lean file; self-contained, book-style. **Start here** for the narrative, then drill into `completeness_relocated.md` / `probe_twist_conic.md`

Cohomology classification extension (3):
- [`cohomology/k_nm_c_classification.md`](cohomology/k_nm_c_classification.md) — universal `(NS, NT, c)` cohomology framework (EnrichedKNSNTc): codim ≥ c parametric + codim ≤ c dual-span + arbitrary-m bilateral kill + K_{3, 3} ↔ Möbius bridges + K_{4, 3} asymmetric instance + cross-graph S-row pattern
- [`cohomology/mediant_cohomology_functor.md`](cohomology/mediant_cohomology_functor.md) — Stern-Brocot mediant Vandermonde at the count level (V 2-term / E 4-term / F factored-Vandermonde²); K_{4, 3} = K_{1, 1} ⊕ K_{3, 2} marquee
- [`cohomology/tripartite_self_containment.md`](cohomology/tripartite_self_containment.md) — K_{2, 1, 3} cohomology (Betti = (1, 0, 0)) + K_{3, 2}^{(c=2)} local (2, 1, 3) signature at every point; cross-frame verdict: atomic-level duality preserved (|E| = |△| = 6), cohomology-level duality broken (b₁ = 8 ≠ 0)

Promotion criteria: `theory/PROMOTION_CRITERIA.md`.
Patterns: `lean/E213/docs/PROMOTION_PATTERNS.md`.
