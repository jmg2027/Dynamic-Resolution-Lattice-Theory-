# Cross-domain: the slot programme ↔ the graded-ladder / π arc (2026-06-11 merge)

Three identifications surfaced by merging main (graded ladder, π
∀-form, crystallographic spectrum) into the slot-programme branch.

## 1. One crystallographic restriction, two scales

Main closed `crystallographic_spectrum` (`FiniteOrderSpectrum`, 29
PURE): finite matrix orders are exactly `{1,2,3,4,6}`.  This branch's
layer-constant story rests on `ImaginaryQuadraticUnitTrichotomy`
(unit-group orders `{2,4,6}`, the `φ(n) ≤ 2` bound, `n ∈ {1,2,3,4,6}`).
**Same set, same mechanism, two scales**: how much circle-torsion an
integer form can hold (slot scale: the phase a layer constant carries
— `i` = ℤ/4, `ω` = ℤ/6) = which rational-angle periods a 2×2 fold
admits (matrix scale).  Slot reading: the finite-order spectrum is
the *phase budget of the pair layer*, and `φ(n) ≤ 2` is its slot
count — a phase is a pair-slot readout, so only torsion whose
cyclotomic degree fits in a pair survives.  Open: state the bridge
(unit order of the integer form = matrix order of its action) as one
theorem connecting the two modules.

## 2. Two escape theorems, one form

Main: `wallis_no_graded_certificate` — some pointings (π's) defeat
*every* schedule; the ∀-form essay reads π as "what every finite
modulus leaves".  This branch: the logarithm escapes *every* finite
slot layer (linear fold-back absence = exponent vectors, a theorem;
nonlinear = Schanuel-open), and "transcendental" is grammar-relative
— each admissible question grammar pins countably many reals.  Common
form: **escape-from-every-X theorems are the sandwich-family boundary
seen from inside X** (X = schedules / slot grammars / moduli).  Open:
is the ∀-form's measure hypothesis (`PiHalfMeasure`) expressible as a
slot-grammar statement (π not pinned by any finite-freedom question
of the polynomial grammar, *effectively*)?  If yes, the two arcs'
boundaries are one definition.

## 3. Modulus degree = certificate depth = answer-axes

Main's `modulus_degree_crossdomain` already identifies modulus degree
with certificate depth one layer up.  The slot programme supplies the
third leg: both equal the **fold-back dimension of the minimal
pinning question** (answer-axes = degree of the irreducible question;
`modulus_degree_ladder`'s algebraic pillar at degrees 2, 3 = the
2-axis `ZSqrt`/φ and 3-axis `CubeRootTwoCut` layers).  Representation
cost (slot count), certificate depth (proof layer), and modulus
degree (completeness layer) are one Lens-property of the pointing
read at three scales.  Open: the three-way identification stated
once, on top of the open four-scale cover-non-surjection schema.
