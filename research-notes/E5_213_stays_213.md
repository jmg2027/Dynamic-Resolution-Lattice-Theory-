# E5 — "213 must stay 213" (User directive, 2026-04-26)

## Directive

> "213 must stay 213.  Use Bishop's work as reference material,
> and then it will actually be easier."

User's framework-philosophical fix: orderProj-Cauchy is the
*primitive*, Bishop's ε-N is *reference only*.

## Resolution of the E4 tension

Design decision for the two Cauchy forms from
`E4_orderproj_vs_epsilon_n.md`:

→ **Adopt orderProj-Cauchy**.  213's native abstraction.

The walls from ε-N Cauchy attempts (E2, E3, E4) arose from
*attempting to import Bishop's framework* — going native bypasses them.

## Implications

### 1. Real213 definition unchanged

Keep existing `Real213 := (xs, HasModulus xs)`.  The
"all-(m, k)-cut stabilization" of orderProj is the definition of the
limit.

### 2. 213-native form of arithmetic

Instead of sequence-level lift (ε-N approach), **cut-level
combination**:

```
def cutSum (cx cy : RealCut) : RealCut := fun m k => ?
```

Bishop's ε/2 trick is a *proof reference*, the primitive is the cut.

### 3. New reading of rational points

The "oscillating around p/q" sequence from E4 is NOT orderProj-Cauchy
→ *external object* to Real213.  Such sequences are ill-defined within
213.

Inhabited in ZFC ℝ (Cauchy in ε-N), absent in 213 ℝ (Real213) → a
second witness that the two ℝ are *truly different mathematical
objects* (first witness from D3 was the Specker sequence).

## Meaning of "it will actually be easier"

When using only 213's native abstraction:

- No multi-layer engineering to try bypassing Bishop's ε-N.
- No StrongModulus / ε/2 trick / reconciliation of the two Cauchy forms.
- All work on the single primitive of orderProj.

→ Conceptual simplicity, framework-internal consistency.

## Path for next session

### F1 — cutSum at RealCut level

`RealCut := Nat → Nat → Bool` (ArchimedeanCauchy already defined).

```
def cutSum (cx cy : RealCut) : Nat → Nat → Bool := fun m k =>
  -- bounded search: ∃ m1 ≤ 2m, cx m1 (2k) ∧ cy (2m - m1) (2k)
  bSearch (2m) (fun m1 => cx m1 (2k) && cy (2m - m1) (2k))
```

### F2 — cut-level monotonicity / properties

cutSum well-defined: monotone in m, etc.

### F3 — Real213 ↔ RealCut round-trip

Real213 → RealCut: (cd : OrderCauchyData xs) ↦ cd.cut.
RealCut → Real213: is the lift possible? — open.

### F4 — Real213 addition via cut

`add (r r' : Real213) : Real213 := lift (cutSum r.cut r'.cut)`.

The possibility of the lift is the key — does every RealCut have a
Real213 origin?

## Cross-references

- `notes/E1_real213_analysis_roadmap.md` (overall plan).
- `notes/E2-E4` (the series of walls).
- `framework/E213/Research/ArchimedeanCauchy.lean` (RealCut definition +
  OrderCauchyData).
- `framework/E213/Research/Real213.lean` (Real213 type).
