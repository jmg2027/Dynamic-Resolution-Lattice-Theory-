# E5 — "213 은 213 해 야 만 해" (User directive, 2026-04-26)

## Directive

> "213 은 213 해 야 만 해.  비숍 친구 의 작업 은 참고 자료 로 쓰 구
> 그 러 면 오히려 더 쉬울 거 임."

User 의 framework-philosophical fix: orderProj-Cauchy 가 *primitive*,
Bishop 의 ε-N 은 *reference 만*.

## E4 tension 의 resolution

`E4_orderproj_vs_epsilon_n.md` 의 두 Cauchy form 의 design 결정:

→ **orderProj-Cauchy 채택**.  213 의 native abstraction.

ε-N Cauchy 시도 의 walls (E2, E3, E4) 는 *Bishop framework 의 import
시도* 에 서 발생 — native 로 가 면 우 회 가능.

## 함의

### 1. Real213 정의 유지

기존 `Real213 := (xs, HasModulus xs)` 그대로.  orderProj 의
"all-(m, k)-cut stabilization" 이 limit 의 정의.

### 2. Arithmetic 의 213-native form

Sequence-level lift (ε-N approach) 대 신 **cut-level combination**:

```
def cutSum (cx cy : RealCut) : RealCut := fun m k => ?
```

Bishop 의 ε/2 trick 은 *증명 reference*, primitive 는 cut.

### 3. Rational point 의 새 reading

E4 의 "oscillating around p/q" sequence 가 NOT orderProj-Cauchy →
Real213 *외 부 object*.  213 안 에 서 그 런 sequence 는 ill-defined.

ZFC ℝ 에 서 는 inhabited (Cauchy in ε-N), 213 ℝ (Real213) 에 서 는
부재 → 두 ℝ 의 *진 짜 다 른 mathematical objects* 의 second
witness (D3 의 첫 witness 는 Specker sequence).

## "오히려 더 쉬울 거" 의 의미

213 의 native abstraction 만 사용 시:

- Bishop 의 ε-N 우 회 시도 의 multi-layer engineering 부재.
- StrongModulus / ε/2 trick / 두 Cauchy form 의 reconciliation 부재.
- orderProj 의 single primitive 위 모든 작업.

→ Conceptual simplicity, framework-internal consistency.

## 다 음 session 의 path

### F1 — cutSum at RealCut level

`RealCut := Nat → Nat → Bool` (이미 ArchimedeanCauchy 정의).

```
def cutSum (cx cy : RealCut) : Nat → Nat → Bool := fun m k =>
  -- bounded search: ∃ m1 ≤ 2m, cx m1 (2k) ∧ cy (2m - m1) (2k)
  bSearch (2m) (fun m1 => cx m1 (2k) && cy (2m - m1) (2k))
```

### F2 — cut-level monotonicity / properties

cutSum 의 well-defined: monotone in m, etc.

### F3 — Real213 ↔ RealCut round-trip

Real213 → RealCut: (cd : OrderCauchyData xs) ↦ cd.cut.
RealCut → Real213: lift 가 가능 한 가? — open.

### F4 — Real213 addition via cut

`add (r r' : Real213) : Real213 := lift (cutSum r.cut r'.cut)`.

Lift 의 가능 성 이 핵심 — 모든 RealCut 가 Real213 origin 인 가?

## Cross-references

- `notes/E1_real213_analysis_roadmap.md` (overall plan).
- `notes/E2-E4` (walls 의 연속).
- `framework/E213/Research/ArchimedeanCauchy.lean` (RealCut 정의 +
  OrderCauchyData).
- `framework/E213/Research/Real213.lean` (Real213 type).
