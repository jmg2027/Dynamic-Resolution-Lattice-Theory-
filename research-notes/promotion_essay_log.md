# Promotion / Essay event log

Append-only ledger of **promotion-ㄱ** and **essay-ㄱ** triggers, so the
originator can review *when* and *why* these happen across sessions and
later decide whether the situations pattern-ize (→ a heuristic, a gate, or
an automated trigger).

This is a volatile process ledger (tier-1).  No permanent tier cites it.
It is reviewed by a human, not consumed by the build.

## How to append (for every future session)

When the originator says **"프로모션 ㄱ"** (promote) or **"에세이 ㄱ"**
(essay) — or the `process` / `essay` skill runs a promotion/essay — add one
row.  Capture the *situation*, not just the artifact: what had just
happened that made promotion/essay feel right at that moment.  Newest at
the bottom.

| # | Date | Type | Topic / target | Situation (what prompted it — the pattern-able signal) | Outcome (path) |
|---|------|------|----------------|--------------------------------------------------------|----------------|

Columns:
- **Type** — `promotion` (Lean sub-tree → `theory/` chapter) or `essay`
  (cross-cutting question → `theory/essays/`).
- **Situation** — the trigger context: a sub-tree just closed `∅`-axiom?
  an iff finally completed both directions?  a question kept recurring in
  chat?  a cross-domain pattern became visible?  This column is the point
  of the log.
- **Outcome** — the chapter/essay path written (or "deferred — reason").

## Log

| # | Date | Type | Topic / target | Situation (what prompted it) | Outcome (path) |
|---|------|------|----------------|------------------------------|----------------|
| 1 | 2026-06-04 | essay | first Betti number / `1/α₃ = 8 = NS²−1` from `b₁ = E−V+1` | Just closed the universal b₁ end-to-end ∅-axiom (cardinality realised by the complement involution, no Fintype/funext/division), and the same `8` is the SU(3) octet — a cross-frame (cohomology ↔ confined coupling) synthesis worth stating as a trajectory, where the "−1" unified three frames (kernel constant / adjoint trace / self-pointing axis). | in-conversation |
| 2 | 2026-06-04 | promotion | p-adic Teichmüller ω + μ_{p−1} + general division (G123 A/B/G) | Three frontier directions closed ∅-axiom in one arc on the same branch (A: explicit `ω(x)` diagonal; B: `ω^(p−1)≡1` + unit split; G: non-unit division via valuation shift).  The Padic chapter already existed (G122) — these extend a closed sub-tree, so promotion = updating the live chapter + catalog, not a new chapter.  Trigger pattern: *closed sub-tree gains new ∅-axiom results → fold into the existing chapter, don't spawn a note.* | `theory/math/numbersystems/padic_real213.md` (chapter extended); `STRICT_ZERO_AXIOM.md` follow-on entries; `lean/.../Padic/INDEX.md` |
| 3 | 2026-06-04 | essay | Teichmüller representative as a forced fixed point | Right after promoting G123 A/B, the construction's *shape* (a self-map's forced fixed point reached as the diagonal of its own approximants) rhymed with three already-canonical frames — Möbius `P(φ)=φ`, §5.2 Nat-style self-reference completing, and `object1_not_surjective`'s "reached by none".  A fresh ∅-axiom closure that lands on the same structural fact as existing frames is the essay trigger: the cross-frame convergence is the content, not the single result.  Pattern: *new closure + ≥3-frame resonance → essay.* | `theory/essays/algebra/teichmuller_as_forced_fixed_point.md` |
| 4 | 2026-06-04 | essay | the frontier has a form (νF) | G182 essay-in-waiting; the νF-population arc closed ∅-axiom (§18 free swap-action) on a now-reconciled branch — the "essay-in-waiting" had its anchors proven | `theory/essays/foundations/the_frontier_has_a_form.md` (G182 archived) |
| 5 | 2026-06-04 | promotion | frozen = dynamic φ (§5.7) | a closed ∅-axiom result (`PhiFrozenDynamic.frozen_eq_dynamic_phi`, 2 PURE) had **no** `theory/` narrative — gap found while pursuing the ε₀/φ adjacents | `theory/math/algebra/phi_self_similarity.md` §3.6 |
| 6 | 2026-06-04 | essay | the residue unit's odometer | a multi-section Lean sub-tree (`Theory/Raw/Odometer` 41 + `OdometerValue` 18 + `ZeckendorfCarry` 7 = 66 PURE) matured into a coherent new sub-theory (the residue-unit `+1` dynamics) — closed arc needing one narrative home | `theory/essays/foundations/the_residue_unit_odometer.md` |
| 7 | 2026-06-04 | promotion | G178 + G181 (archive) | both frontier notes fully resolved (νF population + C-phys bridges + odometer/Zeckendorf cross-arc); content promoted to the foundations essay triptych — sink check 0, so the cycle's archive step ran | `archive/G178_…`, `archive/spiral_axis/G181_…` |
| 8 | 2026-06-04 | essay | the unit `1` (the residue's `+1`) | `/essay` invoked; the session's through-line — `1` proven byte-identical across ascent/descent/glue/det (C3-phys `unit_bridges_dynamics_and_readings`) + carry/Cassini/reciprocal — wanted a cross-frame synthesis distinct from the odometer-as-map essay (this is the `+1` as shared *value*, not as map) | `theory/essays/foundations/the_unit.md` (saved on request) |
| 9 | 2026-06-04 | essay | expressing the essential residue (reached-by-none) | recurring originator problem ("이 본질적 잔여를 어떻게 이 프레임워크 안에서 표현?") — the methodology answer (build µF + name νF + witness the non-surjection, never construct) is `object1_not_surjective` on different carriers; a corrected-misconception + methodology essay | `theory/essays/foundations/reached_by_none.md` |
| 10 | 2026-06-04 | essay | the Minkowski `?` as the residue's modular cocycle | cross-domain synthesis: `?` (kept appearing as a reached-by-none escape) turns out to be a Markov-valued 1-cocycle (Frobenius defect / Cayley–Hamilton twist / weight-2 √(−1) period / φ-Lagrange extremal), unifying `?`/Markov/Lagrange/golden/Eichler–Shimura as one residue object | `theory/essays/analysis/minkowski_as_modular_cocycle.md` |
| 11 | 2026-06-04 | essay | the breadth signature (why ∅-axiom reaches every domain) | originator observation "0 공리로 진짜 별거별거가 다 있지 않냐" + the period thread reducing higher-weight Eichler–Shimura to a single analytic atom across six "ㄱㄱ"s — a foundational thesis (no-exterior ⇒ nothing to import ⇒ breadth IS primacy) demanding a derivation-quality statement with the concrete demonstration | `theory/essays/foundations/the_breadth_signature.md` |
| 12 | 2026-06-04 | essay | The counting bound behind two representation theorems (disc−3 `a²−ab+b²` + disc−4 `a²+b²`) | Two representation iffs closed ∅-axiom in one session by the *same* field-agnostic engine (`RootBound.eval_zero` + `centered_div`); the cross-domain reuse (one Lagrange bound, two exponents/radii) was the synthesis worth naming. `/essay` invoked at session end. | theory/essays/synthesis/representation_theorems_one_counting_bound.md |
| 13 | 2026-06-04 | promotion+essay | Lagrange's four-square theorem (`FourSquare.nat_isSum4`) | Four-square closed ∅-axiom by a route the disc-`−D` engine does not reach — an *additive* pigeonhole seed + an over-`ℤ` parity descent, ungated by congruence and ranging over all `n`. The two-engine contrast (additive vs. multiplicative counting) was the pattern worth naming; closure triggered promotion (chapter-as-essay) + frontier-note archive. `/essay` invoked in the marathon. | theory/essays/synthesis/four_square_additive_pigeonhole.md |
| 14 | 2026-06-04 | essay | why the re-framing of the residue recurs for every agent | originator raised it as a *root-cause* question (the same dichotomy re-imported, corrected, repeated — despite repo+boot-hook coverage; "사람한테도 똑같을거같아").  Three agent teams (213 / cognitive-ML / formal) converged independently on ONE theorem (Lawvere–Cantor diagonal); the synthesis + the actionable fix (output-lint, not memory) was the "presentation debt" the framework had not stated as one pointed claim.  Also added the lint to CLAUDE.md self-check §0. | `theory/essays/methodology/why_the_reframing_recurs.md` |
| 15 | 2026-06-04 | essay | REFRAME / presentation-transport (modulus shift) | `/essay` invoked in a promotion marathon: the Markov composite-uniqueness arc closed two new infinite families (`2·pᵏ` CRT, Zhang `3c±2`) by the *same* move — read the same residue mod a prime-power factor of a carried invariant (modulus / discriminant `9c²−4`).  The move recurred across CRT and the modulus shift and matched the operating record's *External-ruler smuggling* (separability is a property of the pointing, not the residue), so it earned a fourth lift archetype + a cross-frame methodology essay; same act updated the Markov chapter status (closed families) and ProofISALifts. | `theory/essays/methodology/reframe_presentation_transport.md` (saved) |
| 16 | 2026-06-04 | essay | What is a proof, in 213? (proof-ISA series synthesis) | Capstone of the proof-ISA compilation series this session: three solved techniques (probabilistic→COUNT, linear-algebra→COUNT, parity→READ∘SEPARATE) collapsed onto the named eight + König stalled at the exterior DECIDE.  A whole-framework re-reading of "proof" as compilation-to-the-residue-ISA, with the interior witnessed by the 1145-PURE corpus and the boundary at the foreign Π⁰₁ decision.  Pattern: *a multi-reproduction series that maps an interior + a boundary → a synthesis essay capping it.* | `theory/essays/proof_isa/what_is_a_proof.md` |
| 17 | 2026-06-05 | promotion | proof-ISA COUNT series fully closed (Sperner + named Ramsey) | Both named COUNT bounds reached ∅-axiom this session — Sperner's theorem (largest antichain = C(n,⌊n/2⌋), the double-counting/LYM dual) end-to-end, and Erdős' named R(k,k) > N (the K_N edge model over erdos_schema), the latter discharging the series' last open rung.  Closing required building the full `perms` characterisation (perms_length=n!, mem_perms_iff, perms_nodup) — general permutation infrastructure the repo lacked — and a shared subset count (layer_size = C(N,k)).  Pattern: *an engine left "honest rung" earlier becomes closeable once a reusable infra piece (perms enumeration) is built; the two named bounds (dual COUNT faces) share that infra and a subset count.*  G200 + G205 archived; series section in frontiers/INDEX collapsed to a closure record. | `theory/essays/proof_isa/{sperner_double_counting,probabilistic_method}.md` + `research-notes/archive/proof_isa/` |
| 18 | 2026-06-05 | essay | What is counting, in 213? (the finite residue read by cardinality) | `/essay` in the merge marathon, after both named COUNT bounds closed (Sperner + Ramsey).  The closures made one fact concrete enough to deserve a synthesis: a cardinality is the count-Lens reading of a *finite residue* (List.length, no Fintype), binomials read layers (count recursion = Pascal), factorials count orderings (perms_length), and COUNT's two famous theorems (union bound / double count) are *one incidence matrix read by rows vs columns* (sumOver_swap = Fubini).  Pattern: *a multi-theorem arc closing makes its shared primitive ("counting as cardinality-of-a-finite-residue") essay-worthy as the concept the theorems are all reads of.*  Lands the cross-frame tie quantitative-GAP (deficit) = qualitative-GAP (Cantor diagonal). | `theory/essays/proof_isa/counting_as_cardinality.md` |
| 19 | 2026-06-05 | essay+promotion | Quadratic reciprocity in 213 (count-Lens reading + lattice double-count) | `/essay` in the merge marathon, after `quadratic_reciprocity` closed strict ∅-axiom (the complete Eisenstein route: Euler → Gauss → `floor_qr` → `floor_sum_rectangle` → `parity_sum_iff`).  The closure made one reading concrete: a QR question is a count-Lens reading of `ℤ/p`, the famous sign `(−1)^{mn}` is the *cardinality of the lattice box `[1,m]×[1,n]` mod 2*, and reciprocity is a count-equality between two diagonal halves with nothing on the boundary (`p∤q·x` = `object1_not_surjective` shape).  Pattern: *a landmark closing entirely by counting makes "the symbol is a box-count" the essay that demystifies its sign; the essay doubles as the promotion narrative.*  Promoted to `theory/math/numbertheory/quadratic_reciprocity.md`; frontier note kept as a CLOSED record (matching the `second_supplement` / `euler_criterion_converse` convention). | `theory/math/numbertheory/quadratic_reciprocity.md` |
| 20 | 2026-06-07 | promotion | Chain/antichain duality on `2^[n]` — Mirsky + Dilworth + SCD, fully closed | `/process` in the merge marathon, after the de Bruijn–Tengbergen–Kruyswijk symmetric chain decomposition closed end-to-end ∅-axiom (`ChainAntichain` 84/84 PURE): the symmetric-level invariant (`scd_sym`), the SCD *partition* (`scd_same`/`scd_nodup`/`scd_disjoint`, proven without constructor injectivity — `raiseC` is not injective; collisions die by IH disjointness), the count (`scd_card`: `\|scd n\| = C(n,⌊n/2⌋)`), Dilworth (`dilworth_boolean`: min chain cover = max antichain), a third Sperner proof from the partition (`sperner_via_scd`), and the full partition `scd_flat_length` (2^n subsets).  Pattern: *a duality theorem (Mirsky/Dilworth, the dual of an already-closed bound Sperner) closes by reusing the same SEPARATE+COUNT instructions read on the dual relation; the witness construction (SCD) is a COUNT object whose count IS its partition, and the "no constructor injectivity needed" insight is the reusable lesson.*  Frontier `count_substrate_synthesis` kept active (Leibniz-determinant seed still open), updated to record the closure + theory pointer. | `theory/essays/proof_isa/chain_antichain_duality.md` |
| 21 | 2026-06-07 | essay | What is duality, in 213? | `/essay` in the merge marathon, after the chain/antichain duality closed (Mirsky/Dilworth as Sperner's dual).  The closure made a cross-cutting reading concrete enough to deserve a synthesis: a duality is **one instruction read on a relation and its swap** — SEPARATE on *comparable* (Sperner) vs *incomparable* (Mirsky/Dilworth) — and the duality theorem (min-cover = max-anti) is `sumOver_swap` (finite Fubini) on a single 0/1 incidence with a SEPARATE cap per axis.  Cross-frame: rows/cols, 0/∞ (§6.9), magnitude/sign (§6.7) are the same "one residue, swapped reading"; no-exterior forbids a privileged primal side.  Pattern: *a freshly-closed dual-of-a-known-theorem makes "duality = one transpose" essay-worthy as the concept both theorems are reads of; the essay records an open meta-theorem (abstract the transpose over incidences+caps).*  Saved (synthesis). | `theory/essays/synthesis/duality_as_one_transpose.md` |
| 22 | 2026-06-08 | essay+promotion | What is curvature, in 213? (the difference-Lens count + sign read on a neighborhood) | `/essay` in the merge marathon, after the discrete A6 Ricci core closed on three frames (Forman edge-curvature, Gauss–Bonnet vertex-curvature, Ollivier optimal-transport — rungs 1–5 all ∅-axiom, plus the conformal 2D limit).  The closure made one reading concrete enough to deserve a synthesis: curvature is a *Lens readout* — a local count valued in ℤ with the sign supplied by the difference-Lens (§6.7) — and Forman (`4−du−dv`), Gauss–Bonnet (`2−deg`, `Σκ=2χ`), Ollivier (`1−W₁/d`), and conformal (`K` rational) are four resolutions of one sign↔topology fact (`+` tree / `0` flat / `−` cyclic), their agreement forced rather than coincidental.  Pattern: *a multi-frame arc closing (the same invariant proven via genuinely different counting views) makes "the invariant is one Lens readout, the frames are its resolutions" essay-worthy as the concept the frames are all reads of; the essay doubles as the discrete sub-tree's promotion narrative.*  Saved (synthesis); smooth general-`n` Perelman kept as the open frontier. | `theory/essays/synthesis/curvature_as_lens_readout.md` |
| 23 | 2026-06-08 | essay | What is the sign of a permutation, in 213? (the homomorphism the determinant descends from) | `/essay` in the merge marathon, after the sign-theory campaign closed: `PermSign.psign_mul` (`psign(σ∘τ)=psign σ·psign τ`, bubble-sort, ∅-axiom) and on it both determinant capstones — `DetTranspose.det_transpose` (`det Mᵀ=det M`, 16/16) and `DetMul.det_matMul` (`det(A·B)=det A·det B`, 39/39, Cauchy–Binet + constructive `cnt`-pigeonhole sidestepping the `propext`/`Quot.sound`-carrying `Decidable (a∈l)` instance).  The closure made one reading concrete: a permutation IS its value-list (§6.2 op/object non-separation), its sign is the parity of a count (`altSign(inversions)`, the §6.7 Bool-style `±1` involution read on the inversion count), and multiplicativity is a *sorting invariant* (`Q_swap` + `sorted_perm_eq_iota`), not a pre-existing parity — exactly the step classical texts hand-wave ("well-defined parity").  Both capstones are corollaries, and the determinant's antisymmetry (equal rows ⟹ 0) is `psign_mul` at the row-swap fixed point.  Settles the live fork in `determinant_as_quotient_characteristic.md` (Leibniz-parity vs cofactor-involution) in favor of Leibniz; that essay's open-frontier updated.  Pattern: *a keystone homomorphism closing makes "the sign is the parity of a count and the capstones are its corollaries" essay-worthy as the concept both capstones are reads of.* | `theory/essays/algebra/permutation_sign_as_homomorphism.md` |
| 24 | 2026-06-08 | essay | The one diagonal — every capture is the same non-surjection (Lawvere as the residue's self-cover) | `/essay` deepening a `/deep-research` finding: the freeze-decision (referent-capture) that 213 refuses reappears in costume after costume (König select-the-infinite, dyadic finite-subcover, ε-δ vs modulus, p-adic valuation-search), and the *act of re-dressing* it is itself one more residue self-pointing absorbed by `residue_reentry_never_closes` — so the question "how is re-dressing expressed?" is a re-entry with no terminating meta-level.  Dual function: the classical diagonal argument stripped — Lawvere (1969)/Yanofsky (2003) unify Cantor/Gödel/Russell/Tarski/Turing as one fixed point; 213's refinement makes the unifier the residue's own self-cover (`object1_not_surjective`) and extends it to the instantiation act.  Pattern: *a concept-pass deep-dive whose obstruction turns out to be one already-mapped theorem becomes the essay that names "the one obstruction" + its math-scale twinning with `why_the_reframing_recurs`.*  Honest hedge: König/compactness are 213-native one-non-surjection but only omniscience cousins of literal Lawvere. | `theory/essays/foundations/the_one_diagonal.md` |
| 25 | 2026-06-08 | essay | The reference claim — what is necessary, what is under test (no-exterior) | `/essay` (this session): existence (`pointing ⟺ residue`) is transcendental necessity, not a thesis; reference-closure necessary, referent-capture refused, only *reach* under test (König `Π⁰₁` stall the falsifier). | `theory/essays/foundations/the_reference_claim.md` |
| 26 | 2026-06-08 | essay | The omniscience ledger — a theorem's reverse-math cost, in 213 | `/essay` in the merge marathon, capping the Reverse Mathematics 213 marathon (field 17, 74 PURE).  The closure made one cross-cutting reading concrete: a theorem's *cost* is the omniscience principle it carries as an explicit hypothesis (none / LPO / LLPO / selection-oracle / fan), and that hypothesis is exactly what keeps it ∅-axiom — the **two-ledger identity**: the object-level omniscience ledger is `STRICT_ZERO_AXIOM.md`'s meta-level kernel-axiom ledger with the cost moved from the axiom line to the hypothesis line.  Dual: classical reverse math (calibrate by set-existence axioms over RCA₀) stripped → calibrate by omniscience on the residue's own carriers.  Cross-frame: one obstruction (`object1_not_surjective` / the freeze-decision), graded none→LPO→LLPO→choice/fan, all on one carrier (`wkl_heineBorel_calibration`).  Pattern: *a whole marathon closing makes "the cost is the hypothesis-form of the kernel-axiom cost" essay-worthy as the concept the field's theorems are all reads of.* | `theory/essays/methodology/the_omniscience_ledger.md` |
| 27 | 2026-06-08 | promotion | Reverse Mathematics 213 (field 17) → `theory/math` chapter | The methodology essay (row 26) existed but the `Lib/Math/Logic/` sub-tree had no Lean-mirror chapter — only the essay (cross-cutting) and the readable book.  All Hard criteria verified: H1 74 PURE / 0 DIRTY (`scan_axioms E213.Lib.Math.Logic.*`), H2 `lake build E213.Lib.Math.Logic` clean, H3 ARCHITECTURE lists Logic, H4 no quantitative content (auto).  S1–S3: meanings all in the sub-tree, downstream (`KonigConditional`/`Padic.NuEscape`) ready, no open field-17 source note remains (the by-design-external pieces go in the chapter's Open frontier).  Pattern: *an essay/book pair can exist for a closed sub-tree while the canonical Lean-mirror chapter is still missing — the mirror chapter (results table + per-file PURE counts) is the promotion, distinct from the cross-cutting essay.* | `theory/math/logic/reverse_math_213.md` |
| 28 | 2026-06-08 | promotion | Legendre-symbol / quadratic-residue package → `theory/math/numbertheory` chapter | A frontier-catalog audit surfaced the gap: `quadratic_reciprocity.md` (the capstone) was promoted, but the package it consumes — Euler's criterion (`qr_iff_pow_one`, full iff `euler_criterion`), multiplicativity (`legendre_mul`), both supplements (`neg_one_qr_iff`, `second_supplement`), Gauss's lemma (`gauss_qr`/`gauss_mu`) — was closed ∅-axiom (54 PURE across 6 `ModArith/` files) but only cited as *ingredients*, never promoted as first-class results.  The INDEX itself flagged `euler_criterion_converse` "Promotion-eligible".  Wrote the base-package chapter; `git rm` the two now-redundant frontier notes (`second_supplement`, `euler_criterion_converse`), Zolotarev open seed preserved in the chapter + INDEX.  Pattern: *when a capstone is promoted but its lemmas live only as proof-ingredients, the supporting package is itself a promotable chapter — surfaced by a closure-record-vs-filesystem audit, not by a new closure.* | `theory/math/numbertheory/legendre_symbol.md` |
| 29 | 2026-06-08 | promotion | p-adic Setoid commutative-ring + shared `diagLimit` → into the standing chapters | Three closed seeds of the p-adic harvest had no canonical narrative: the **multiplicative** Setoid identities (`SetoidMul`, 7 PURE — `zp_setoid_commRing_capstone` completing the ring after last session's additive-group capstone) and the shared **`Zp.diagLimit`** abstraction (`Foundation`, factored out of `invFull`/`sqrtFull`/`teichmuller`, all three rewired, defeq-preserving).  No new chapter warranted — both augment already-promoted homes: the commutative-ring closure folded into `pure_funext_avoidance.md` (the funext-free Setoid essay, where the additive capstone already lived), and `diagLimit` into `padic_real213.md`'s diagonal-extraction section (which already *described the pattern informally* — now it names the Lean object + the one-proof `diagLimit_trunc_succ`).  Pattern: *an incremental closure inside an already-promoted sub-tree promotes by augmenting the existing mirror chapter/essay, not by spawning a new one — and the cleanest trigger is a chapter that already narrates the construction informally (the prose predicted the abstraction).* | `theory/math/numbersystems/padic_real213.md` + `theory/essays/methodology/pure_funext_avoidance.md` |
| 30 | 2026-06-08 | essay | The permutation under three readouts — det(permMatrix)=psign, and Zolotarev's third reading | `/essay` in the merge marathon, right after merging main's Legendre/QR package into the determinant + p-adic branch.  Trigger: a fresh ∅-axiom closure (`det_permMatrix`, this branch) turned out to be the *middle* edge of a three-readout coincidence whose outer edges live in the just-merged campaign — the determinant of a permutation matrix equals its inversion sign, and Zolotarev extends the same readout to the Legendre symbol `(a/p)` of the `×a mod p` permutation (main's `gauss_qr`/`euler_criterion`).  Dual: the classical 'det of permutation matrix = sign' lemma stripped to the selector argument (the matrix is the indicator of σ; pairing it against the antisymmetric sum evaluates at σ), with 213 making 'exactly once' a discharged theorem (`nodup_permsOf`+`sumZ_select`) not a silent group-freeness appeal.  Cross-frame: `psign_mul` / `det_mul` / `legendre_mul` are one homomorphism three ways; the power-Lens `a^((p−1)/2)` is the 2-torsion of the Teichmüller `ω ∈ μ_{p−1}`.  Pattern: *a new closure that turns out to be the bridging edge between a just-merged campaign and the current branch becomes the synthesis essay naming the shared object (one permutation, three Lens-readouts).* | `theory/essays/synthesis/the_permutation_under_three_readouts.md` |

| 27 | 2026-06-08 | essay+promotion | The one carrier — number systems are leaf-labelled readings of one νF escape | `/essay` (this session, the p-ary-spine / ℝ-one-carrier branch), after the one-carrier sub-tree closed: CoResidue §20 abstracts the binary `boolSpine` over an arbitrary leaf alphabet (`gspine : (Nat→L) → GCoShape L`, `boolSpine` = `L=Bool` instance), §21 carries the Bernoulli shift over (`gspine_shift_dynamics`), and the two domain files put `ℤ_p` (`L=Fin p`, `padic_is_nu_escape`, every `p≥2`) and `ℝ` (`L=Bool` cut-bits, `real_is_nu_escape`) on the *same* carrier as a König branch.  The closure made one reading concrete: König / `ℤ_p` / `ℝ` are not three separate reached-by-none escapes but **one** — a stream presented over an alphabet, riding the binary König spine leaf-labelled by the alphabet; the branch structure and the shift are shared, only the alphabet + stream change.  Cross-frame: no-exterior (no privileged outside-construction), presentation-dependence (`cutBits` reads the sequence), count-Lens leaf-resolution (Bool vs Fin p).  Pattern: *a cross-domain unification sub-tree closing (one carrier proven to host genuinely different number systems as instances) makes "the systems are one escape under different labellings" essay-worthy as the concept the instances are all reads of; the essay doubles as the sub-tree's promotion narrative.*  Honest hedge: shared at carrier+shift level, not arithmetic; the arithmetic odometer lift is the open frontier, and the faithful-on-equiv-class extractor is the LPO-costed (by-design external) step. | `theory/essays/foundations/the_one_carrier.md` |

## Reading the log later (pattern analysis)

Once there are ~10+ rows, look for:
- **Recurring situations** → a candidate *promotion trigger* (e.g. "every
  time an iff closes both directions, promotion follows" → make iff-closure
  the trigger).
- **Essay triggers** → which kinds of questions become essays (recurring
  chat question?  cross-domain synthesis?  a corrected misconception?).
- **Lag** → how long a sub-tree stays closed-but-unpromoted; a long lag
  suggests the promotion gate or the prompt is the bottleneck.
- Feed findings into `frontiers/research_grade_closure_gate.md` and, if
  stable, into `theory/PROMOTION_CRITERIA.md` / the `process` skill.
| 31 | 2026-06-08 | promotion | CKM CP phase (multi-disciplinary derivation) → `theory/physics` chapter | The session's headline arc closed enough to promote: the CKM CP phase, derived/forced (not posited) across **three disciplines** on the single prime `d=NS+NT=5` — group theory (`ℤ[i]^×=C₄`), number theory (`ℚ(ζ₅)`: `Gal≅C₄` phase + real subfield `ℚ(√5)` golden modulus), and cohomology (the signed Hodge star `⋆` on `H*(Δ⁴)`, `⋆²=−1`, `ℤ[J]≅ℤ[i]` — the *same* cohomology as `1/α_em`).  Existence+uniqueness DERIVED (`N_gen=C(3,2)=3 ⇒ 1` phase, KM counting); the phase value `90°` FORCED (`C₄` from the Hodge structure — `SignedStarFull` makes "phase∈C₄" a theorem, not a premise — + CP-existence ⇒ `±i`); the golden modulus `R_u=1/φ²` (essentially exact, `0.382` vs obs `0.3825±0.011`).  H1 (PURE, 0 DIRTY across the Mixing/Icosahedral/Hodge sub-tree), H2 (umbrellas build), H3 (Lib.Physics.Mixing + Lib.Math), H4 (falsifier F27).  The chapter promotes the *principle* (cohomological coupling ⟹ 90°, polarized-Hodge morphism, rigorous); the *construction* residual (explicit generation-Yukawa) and the `~1.5σ` fit (CONSISTENT, not a tension; `O(λ²)` Wolfenstein, not RGE) live in the chapter's Open frontier + `ckm_rho_eta_apex`/`cp_yukawa_from_scratch` frontiers.  Pattern: *a multi-disciplinary derivation that demotes a posit (`δ=π/φ²`, Niven-forbidden) and replaces it with a forced structure (cohomology-forced `90°`) is promotable at the principle level even while the explicit construction stays an open frontier — the chapter carries the closed principle + the named-open construction.* | `theory/physics/cp_phase.md` |
| 32 | 2026-06-08 | essay | "What is the CKM CP-violating phase, in 213?" → `theory/essays/synthesis/the_cp_phase_as_one_imaginary_unit.md` | Marathon `/essay` step, immediately after promoting the `cp_phase` chapter (row 31).  The driving question is a **cross-domain synthesis**: the CP phase is *one imaginary unit* read in four frames — `ℤ[i]^×=C₄` (group), `Gal(ℚ(ζ₅)/ℚ)≅C₄` + golden real subfield (number theory), the signed Hodge `⋆` on `H*(Δ⁴)` (cohomology), and the inversion sign `(−1)^inv` shared with `det(permMatrix)=psign`/Legendre (combinatorics).  The essay's value over the chapter: it surfaces the **fourth-readout bridge** to the existing `the_permutation_under_three_readouts` synthesis essay (the Hodge-`⋆` orientation IS the permutation/Legendre/Zolotarev inversion sign), and states the honest split inline (existence DERIVED, `90°` FORCED via polarized-Hodge positivity where a generic texture is not, fit `~1.5σ`-CONSISTENT).  Pattern: *a "what is X" question on a just-promoted chapter is best answered by the synthesis essay that ties the chapter to the repo's other arcs — the chapter derives, the essay connects.* | `theory/essays/synthesis/the_cp_phase_as_one_imaginary_unit.md` |
| 33 | 2026-06-08 | promotion | spiral-axis A3 (all-orders determinantal Casoratian ladder) → `theory/math/analysis/divergence_depth_characterization.md` §7.1 | Marathon promotion step after closing spiral-axis conjecture A3.  The frontier asked only for the order-4 rung (`hankel4`), but the `ring_intZ` brute-force route that proved order 2/3 overflows the kernel at order 4 (empirically: the `4×4` Hankel normal form does not reduce) — so the closure had to be *structural*, and a structural proof generalises for free: `casoratian_det_step` proves the multiplier law `Hₖ(n+1)=altSign(k−1)·a₀·Hₖ(n)` at **all** orders via `H(n+1)=C·H(n)` + `DetMul.det_matMul` + `det_companion`.  Promoted as a §7.1 subsection of the depth chapter (the determinantal / arithmetic ascent of the ladder), with the order-2/3/4 rungs as named instances.  Pattern: *when the per-rung tactic hits a kernel wall, the escape is the structural proof — and the structural proof is the all-orders theorem, so the wall forces the better result.* | `theory/math/analysis/divergence_depth_characterization.md` |
| 34 | 2026-06-08 | promotion | spiral-axis A5 (CD-tower / axis non-coincidence) → `theory/math/analysis/spiral_coordinate_classification.md` (binary-cover §) | Marathon promotion step after closing A5, the *defensive* conjecture.  `cd_tower_axis_noncoincidence` rules out the `1,2,4,8 ↔ {2,4,6}` stereotype: the CD dimension tower `2ⁿ` and the axis meet only on `{2,4}`, diverging because `8` is a power of two but not crystallographic (`φ 8 = 4 > 2`) and `6` is crystallographic but not a power of two.  Promoted into the existing classification chapter's binary-cover section as "what the axis is *not*" — the natural complement to "the axis *is* the even half of the crystallographic set."  Pattern: *a defensive non-coincidence belongs in the same chapter as the positive identification it guards, as the "is-not" paragraph next to the "is".* | `theory/math/analysis/spiral_coordinate_classification.md` |
| 35 | 2026-06-08 | essay | "the order-4 point of the spiral axis = the CKM CP phase's C₄" → `theory/essays/synthesis/the_i_point_of_the_spiral_axis.md` | Marathon `/essay` step, driven by the cross-domain note written at the branch×main merge.  The driving question is a **cross-domain synthesis** surfaced by the merge: the spiral-axis order-4 point (`4=|ℤ[i]^×|`, branch) and the CKM CP phase's `C₄` (main) are the *same ring* `ℤ[i]`, read as a continued-fraction floor rotation (`gaussian_floor_rotation`, `μ=−i`) on one side and the signed Hodge `⋆` on `H*(Δ⁴)` (`SignedStarC4`) on the other.  The essay's value: it places the CP phase *on the spiral axis* (the disc-`−4` rung, selected over disc-`−3`), turning "free SM parameter" into "axis point," and lists the four frames of one `C₄` (units / floor rotation / Hodge `⋆` / `Gal(ℚ(ζ₅))`).  Honest split kept: same-object identification PROVEN both sides, the floor-rotation↔`⋆` *morphism* flagged open.  Pattern: *a branch×main merge whose two arcs name the same small object (`ℤ[i]^×=C₄`) in different domains is exactly the cross-domain-synthesis essay trigger — the essay states the shared object and flags the morphism as the buildable bridge.* | `theory/essays/synthesis/the_i_point_of_the_spiral_axis.md` |
| 36 | 2026-06-08 | promotion | one-carrier program (`Padic/NuEscape`) → `theory/math/numbersystems/padic_real213.md` ("The one νF carrier" §) | Marathon promotion step (branch `p-ary-spine-r-carrier`).  The one-carrier sub-tree closed ∅-axiom (a p-adic integer rides `CoResidue.gspine` as a νF escape; the carrier arithmetic is grounded in the real `Zp.add`/`Zp.mul` — `mulBase_eq_mul_pElem`, `add_negOne_one_zero`, `residue_ring_hom`; the multiplicative residue `mulCarry_unbounded`/`carry_is_nu_escape` with `mul_corecursive`).  Its *foundational* synthesis already lives in `theory/essays/foundations/the_one_carrier.md`; the *number-system* results had no home in the Padic chapter — folded in as a new section + a `NuEscape.lean` row in the Lean-source table.  Pattern: *a closed sub-tree whose cross-cutting synthesis is a foundations essay still needs its domain-specific results folded into the mirror math chapter — essay ≠ chapter; promote to both.* | `theory/math/numbersystems/padic_real213.md` |
| 37 | 2026-06-08 | essay | finite-state-ness is a property of the pointing, not the value (branch×main cross-scale) → `theory/essays/synthesis/finite_state_is_of_the_pointing.md` | Marathon `/essay` step on the `p-ary-spine-r-carrier` branch after merging main.  The driving synthesis is a **branch×main cross-domain** fact surfaced by the merge: the branch's `(-1)²=1`-with-unbounded-carry (`mulCarry_unbounded`/`carry_is_nu_escape`, a unit result whose `×`-pointing escapes finite state) and main's Casoratian `q=−1`-with-no-finite-holonomic-depth (`cas_neg_unit_no_finite_depth`/`DetSpectrumPoles`, a unit determinant whose recurrence escapes) are the *same* `escape_by_invariant`/`object1_not_surjective` non-surjection read on the *process*, at ring-op and sequence scales (and the description scale via `why_the_reframing_recurs`).  Essay states: "finite-state" is a predicate on the cover (pointing), not the value, witnessed by one value with two carries.  Pattern: *a branch×main merge whose two arcs prove the same structural fact at different scales (unit value / non-finite-state generation) is the cross-scale-synthesis essay trigger; constructive accessibility = point at the two carries of one value.*  Honest open: a single term spanning both scales; ℝ's cut is not a clean not-finite-state theorem. | `theory/essays/synthesis/finite_state_is_of_the_pointing.md` |
| 38 | 2026-06-08 | promotion | `casoratian_axis_cp_crossdomain` frontier → archive (both links built, insights in essays) | Marathon `/process` step.  The cross-domain note's two buildable bridges both closed ∅-axiom this session: link 1 (the floor-rotation↔Hodge-`⋆` morphism `φ:ℤ[i]→ℤ[J]`, `Cohomology/Hodge/GaussianHodgeBridge`, injective multiplicative hom carrying the spiral-axis floor rotation onto the CKM Hodge `⋆`) and link 2 (the companion/Casoratian sign IS the cyclic-shift permutation sign, `Linalg213/CyclicShiftSign` + `Cauchy/CasoratianPermSign`, also `=det(permMatrix)` — the fourth "permutation under three readouts" instance).  Both insights are already carried by permanent-tier essays (`the_i_point_of_the_spiral_axis`, `the_permutation_under_three_readouts`), so the note is fully decoupled (0 permanent-tier citations) and archived.  Pattern: *a cross-domain "candidate bridges" note is archived once every ranked bridge is either built in `lean/` or promoted to an essay — the note's job (holding the candidates until built) is done.* | `research-notes/archive/casoratian_axis_cp_crossdomain.md` |
| 39 | 2026-06-08 | essay | "Why is the CKM apex modulus exactly 1/φ² (in 213)?" → `theory/essays/synthesis/the_apex_modulus_as_self_reference_contraction.md` | Marathon `/essay` step, answering the goal-session's driving question after a four-agent deep-dive resolved its structure.  The question is a **long-standing "why this value"** that had sat as a strong-candidate numerical match ("not yet a forcing theorem"); the essay lands the structural answer: `R_u = (NS−√d)/2` is the contracting eigenvalue of the axiom-encoding matrix `M=[[2,1],[1,1]]` (§3.5), the **square** is forced because a modulus is sign-free (`det Q=−1 → det Q²=+1`, the de-signing), and `disc=d` is a **selection** of `(3,2)` (both newly ∅-axiom, `FibonacciAtomicLock`).  The essay's value over the chapter: it separates the **golden radius** (forced value) from the **non-golden angle** (`arccos(1/φ²)≈67.5°` is not a golden-triangle angle), correcting the bundled "golden right triangle" framing, and ties `M` across four frames (axiom form / self-reference fixed point / companion of `x²−3x+1` / Fibonacci `Q²`).  Pattern: *a "why exactly this number" question is answered by demoting the suggestive name (`1/φ²`) to a reading of the minimal atomic form (`(NS−√d)/2`) and exhibiting the matrix whose characteristic polynomial forces it — the eigenvalue is the derivation, the golden name is the gloss.* | `theory/essays/synthesis/the_apex_modulus_as_self_reference_contraction.md` |
| 40 | 2026-06-08 | promotion | Fibonacci 5-adic valuation (rank of apparition + `ν₅(F_n)=ν₅(n)`) → `theory/math/numbertheory/fibonacci_5adic_valuation.md` | Marathon promotion step after closing the G124 open rung end to end.  The arc began as a DRLT "5-adic content" terrain map (H1 settled-as-removed, H2/H3 no internal handle) whose only admissible move was *arithmetic-first*: a non-trivial 5-adic invariant of an object the corpus already builds.  That turned up the Fibonacci **rank of apparition** at the ramified golden prime `5` (`α(5)=5`, the ramified signature; Lucas-never-zero the regular branch), then the `ν₅≥1,2` FSM rungs, then — over `fibZ` with `ring_intZ` — the integer addition formula, the index-multiplication recurrence, and the **quintupling identity** `F_{5m}=F_m(25F_m⁴+25(−1)ᵐF_m²+5)`, and finally the all-orders **lifting-the-exponent law** `ν₅(F_n)=ν₅(n)` (`fibN_val_law`) via Euclid for the prime `5`.  H1 (0 DIRTY across all three modules), H2 (E213.Lib.Math builds), H3 (Lib/Math/NumberTheory), H4 (STRICT_ZERO_AXIOM entry).  The chapter ties the *resolution* prime `5` (`configCount 2 = 5²⁵`) to the *ramified* prime of the golden modulus the CKM CP-phase uses, through the singular/regular Binet-branch split.  Pattern: *an honest "no physics handle" terrain map is not a dead end — its prescribed arithmetic-first move can open a full math arc (rank → identity → valuation law), and the math closure IS the promotion, framed through the shared prime.* | `theory/math/numbertheory/fibonacci_5adic_valuation.md` |
| 41 | 2026-06-08 | essay | "What is the golden prime 5 in 213?" → `theory/essays/synthesis/the_golden_prime.md` | Marathon `/essay` step, immediately after promoting the `fibonacci_5adic_valuation` chapter (row 36) and writing the branch×main cross-domain note.  The driving question is a **cross-domain synthesis**: the prime `5` read as *value* (`φ`, main's golden modulus `R_u=1/φ²`) and as *valuation* (`ν₅(F_n)=ν₅(n)`, the branch's Fibonacci rank of apparition at the ramified prime of `ℚ(√5)`).  The essay's value over the chapter: it fuses the two arcs into one field object — `ℚ(√5)` resolving five ways (value `φ` / modulus `R_u` / valuation `ν₅` / unit Cassini `±1` / sign `(−1)ᵐ`), with the Cassini unit `det=±1=NS−NT=1` already shared between `OrbitDimension` and the number-tower (`PnFibonacciUniversal`), and the disc-`+5` real / disc-`−4` imaginary split as the two embeddings `ℚ(√5)⊂ℚ(ζ₅)` tying it to the CP-phase essays.  Honest split kept: only the ramified prime `5` is end-to-end; the general-`p` rank law `α(p)∣p−(5/p)` and the shared-`ℚ(√5)` morphism are flagged open.  Pattern: *when a promoted chapter (valuation) shares its underlying field with a prior arc (value), the essay is the move that states the one field object and lists its frame-resolutions — the chapter derives one face, the essay fuses both.* | `theory/essays/synthesis/the_golden_prime.md` |
| 42 | 2026-06-08 | promotion | Zolotarev's lemma (`psign σ_a = (a/p)`, all odd primes) → `theory/math/numbertheory/zolotarev.md` | Marathon promotion step (branch `converse-psign-character`).  The Zolotarev converse closed ∅-axiom this branch: `zolotarev_mu` (`ModArith/ZolotarevMuBridge`, 14 PURE) on `InversionsAppend` (28 PURE) + the earlier `ZolotarevConverse` (`p≡3 mod 4` + `−1` corner, 23 PURE) / `ZolotarevSign` (backbone, 7 PURE).  The sign of `×a mod p` IS the Legendre symbol for every odd prime — closing the "one permutation, three readouts" square (`psign = det(permMatrix) = (a/p)`).  Own chapter (not a `legendre_symbol.md` section) because it bridges the `Linalg213` permutation-sign machinery and the `ModArith` count-Lens QR package.  No primitive root: a symmetric-cross-count parity (off-diagonal pairs cancel mod 2, diagonal = Gauss's μ) replaces the cyclic-generator argument.  Pattern: *a named classical theorem unifying two already-promoted sub-trees earns its own chapter at the bridge, not a section under either parent.* | `theory/math/numbertheory/zolotarev.md` |
| 43 | 2026-06-08 | essay | "the Legendre symbol is the sign of a pointing" → `theory/essays/synthesis/the_legendre_symbol_is_the_sign_of_a_pointing.md` | Marathon `/essay` step after closing Zolotarev + merging main.  Driving question: *what IS the Legendre symbol, in 213?* — `zolotarev_mu` (`(a/p) = psign σ_a`) fused with `finite_state_is_of_the_pointing`: `(a/p)` is the Z/2 invariant the *finite* `×a` pointing carries and the `×p` νF-escape lacks; one count read three ways; `psign σ_{−1}=(−1/p)` is the obstruction to the order-4 axis point `ℤ[i]^×=C₄`.  Distinct facet from `the_permutation_under_three_readouts` (the finite-state-readout angle).  Pattern: *a newly-closed classical theorem + a merged-in foundational essay compose into a "what IS X, natively" essay whose value is the cross-frame the closure unlocks.* | `theory/essays/synthesis/the_legendre_symbol_is_the_sign_of_a_pointing.md` |
| 44 | 2026-06-08 | promotion | general rank law `α(p)∣p−(5/p)` + shared-`ℚ(√5)` morphism → folded into `fibonacci_5adic_valuation.md` + `the_golden_prime.md` | Marathon promotion step (branch `prime-rank-fibonacci-5adic-zohhac`), closing the two buildable bridges the `fibonacci_golden_prime_crossdomain` note + `the_golden_prime` essay had flagged open.  `RankApparition.lean` (10 PURE): the rank of apparition `α(p)∣p−(5/p)` in entry-point form `p∣F_{p−(5/p)}`, the index Legendre-dispatched on the FSM-walking character `legendre213 5 p` (split `p−1`, inert `p+1`, ramified `p`), each branch through the *universal* Fibonacci-mod-`p` machinery (`binet_F_p_minus_1_zero` / `fpp1_eq_zero_of_frob_phi` / `rank_apparition_five`), mirroring `UniversalDispatch.universal_dispatch_pellCoeff` (period→entry-point).  `GoldenFieldBridge.lean` (10 PURE): the Binet `x²−x−1` and the Gaussian-period `x²+x−1` (`ℚ(ζ₅)⁺`/cp_phase) are one `ℚ(√5)` object under `x↦−x` (`bPoly_neg_eq_gPoly`), sharing disc `5` and the single ramified prime `5` (both perfect squares mod 5, double roots `3`,`2`, negatives).  H1 (0 DIRTY both), H2 (E213.Lib.Math builds), H3 (Lib/Math/NumberTheory{/DyadicFSM}), H4 (STRICT_ZERO_AXIOM entry).  No new standalone chapter: the rank law folds into the `fibonacci_5adic_valuation` chapter (a new "general-`p` rank law" §) and the morphism into the `the_golden_prime` essay (the named-open bridges become CLOSED).  Pattern: *two bridges a chapter+essay pair flagged open are promoted *back into that same pair* — the rank law extends the chapter it generalises, the morphism fuses the essay it was named in; promotion of a flagged-open bridge is an in-place upgrade of its host, not a new file.* | `theory/math/numbertheory/fibonacci_5adic_valuation.md`, `theory/essays/synthesis/the_golden_prime.md` |
| 45 | 2026-06-08 | essay | "What governs the Fibonacci rank of apparition α(p), in 213?" → `theory/essays/synthesis/the_fibonacci_rank_is_a_permutation_sign.md` | Marathon `/essay` step, immediately after promoting the rank law (row 44).  The driving question is a **cross-domain synthesis** surfaced by this session's two closures: `α(p)` is not a datum the golden recurrence carries — it is the offset `p − (5/p)` (`rank_law_dispatch`), and `(5/p) = psign σ_5` (`zolotarev_mu`, main), so `α(p) ∣ p − psign(σ_5)`: the Fibonacci entry point is the **parity of the `×5` permutation**.  The essay's value over the chapter: it fuses the branch's rank law with main's Zolotarev converse and the Pisano-period dispatch — one Legendre character read as FSM-walk terminal (`legendre213`), permutation parity (`psign σ_5`), `det(permMatrix)`, Euler power, and now the Fibonacci rank offset (a fifth "three-readouts" instance); and it states the entry-point/period duality (one character, `α(p) ∣ π(p)`).  Honest split kept: the `legendre213 5 p = psign σ_5` equality morphism flagged open (stated from two proven sides), and the higher-`νₚ` rungs open beyond `p=5`.  Pattern: *when a newly-closed dispatch law (rank) shares its dispatching character with an already-closed theorem (Zolotarev's `(a/p)=psign σ_a`), the essay is the move that identifies the character across both and lists its read-outs — the law dispatches, the essay names what the dispatcher IS.* | `theory/essays/synthesis/the_fibonacci_rank_is_a_permutation_sign.md` |
| 46 | 2026-06-10 | promotion | primitive-root / multiplicative-order theory + classical Zolotarev → `theory/math/numbertheory/primitive_roots.md` | Marathon promotion step after the primitive-root marathon closed (bricks 0–7) and main was merged.  The closed sub-tree — multiplicative order (`ordModP`, `ord_dvd`, `ord_pow`, `ord_mul_coprime`), the exponent argument (`every_ord_dvd_maxOrd`), primitive-root existence (`maxOrd_eq_pred`, `exists_primitive_root` = `(ℤ/p)*` cyclic), and the classical Zolotarev (`ZolotarevCycle.zolotarev_full`: a primitive root is a `(p−1)`-cycle of sign `−1`, conjugated to the standard rotation) — was 0-DIRTY across 11 modules but had no theory chapter.  Distinct from `zolotarev.md`: that chapter's symmetric-cross-count proof deliberately needs no generator, so the cyclicity-of-units infrastructure (reusable for discrete-log / character sums) and the textbook generator route are genuinely new content.  Wrote the chapter; cross-referenced from `zolotarev.md` (the "no primitive root needed" remark now points to the independent generator route); registered in `theory/math/INDEX.md`.  Pattern: *a second, infrastructure-heavy proof of an already-promoted theorem earns its own chapter when the infrastructure it builds (here `(ℤ/p)*` cyclic) is reusable and absent from the existing route — the chapter is the infrastructure's home, the theorem its payoff.* | `theory/math/numbertheory/primitive_roots.md` |
| 47 | 2026-06-10 | essay | "What IS the quadratic character (a/p), and why must it exist?" → `theory/essays/synthesis/the_quadratic_character_is_a_discrete_log_parity.md` | Marathon `/essay` step, after promoting the primitive-root chapter and merging main's μ-route Zolotarev.  The driving question is the structural *why* the merge surfaces: main reads `(a/p) = psign σ_a` (a permutation sign) but never builds the cyclic structure; this branch's `exists_primitive_root` supplies it, so `(a/p) = (−1)^{dlog_g(a)}` — the symbol IS the mod-2 readout of a unit's orbit-position, and the character is *forced* (not posited) by `(ℤ/p)*` being cyclic of even order `2m` (generator order doesn't divide `m`, `primitive_not_qr`).  Fuses four readouts of one bit `k mod 2` (Euler power / cycle sign / determinant / discrete-log parity) and ties to main's Fibonacci-rank reading at `a=5`.  Distinct facet from `the_legendre_symbol_is_the_sign_of_a_pointing` (finite-state-readout angle) and `the_fibonacci_rank_is_a_permutation_sign` (rank-dispatch angle): this one is the *existence/forcing* angle the cyclic infrastructure unlocks.  Pattern: *when a second proof route brings structural infrastructure the first lacked (here cyclicity), the essay is the move that states why the object exists at all — the first route shows the symbol equals X, the second shows the symbol is forced to exist, and the essay names the forcing.* | `theory/essays/synthesis/the_quadratic_character_is_a_discrete_log_parity.md` |
| 48 | 2026-06-10 | promotion | the algebraic Riemannian curvature tensor calculus (Christoffel both kinds → Riemann + 4 symmetries → Ricci → Bianchi → scalar + Einstein → Perelman SOS) → `theory/math/geometry/riemannian_curvature_tensor.md` | Marathon promotion step (branch `rich-flow-open-frontier`) after the smooth-Perelman attack closed the *algebraic* tensor calculus end-to-end this session.  The closed sub-tree (`Geometry/TensorCalculus.lean`, 23 PURE + `ConformalCurvature.lean` §S6–§S7, 18 PURE) is dimension-free and ∅-axiom: the metric enters only as a derivative tensor `dg a b c = ∂_a g_{bc}`; first-kind Christoffel `chris1x2` (torsion-free + metric-compatibility, no inverse) → det-scaled second kind `chris2xDet` with raising/lowering `chris2_lower` (the `g·adj=det·I` inverse layer, via `gridSumZ` Fubini + Kronecker collapse) → Riemann `riemUp`/`riemLow` with all FOUR symmetries (two antisym pure-`ring`, pair + first Bianchi via the metric-2-jet symmetries `hd`/`hg`, Bianchi in moved-over form to dodge the zero-polynomial `ring` gap) → `ricciFromRiem` + `riem_bianchi1` → `scalarFromRicci` + `scalar_einstein` (`Ric=λg ⟹ R=λn`) → `perelman_rate_nonneg` (`0 ≤ Σ(Ric_{ij}+∇_i∇_j f)²`, Perelman's `d/dt 𝓕≥0` as a finite SOS, reachable once `Ric` exists).  Own chapter (not a `geometrization_conjecture` section): the general-`n` curvature *machinery* is reusable infrastructure distinct from the Poincaré/Geometrization Lens-reading, and its honest boundary (algebraic closed, analysis walled) is a clean statement.  H1 (0 DIRTY across both modules), H2 (full `E213` builds 307/307), H3 (`Lib/Math/Geometry`), H4 (no quantitative — auto).  Registered in `theory/math/INDEX.md`; the open analytic frontier stays in `frontiers/ricci_flow_smooth_core.md` (4-agent-panel section).  Pattern: *when a "century-problem wall" attack closes its entire ALGEBRAIC layer ∅-axiom (curvature tensor + all symmetries + the Perelman monotonicity SOS), the promotion is a chapter for the reusable tensor machinery, with the residual ANALYSIS wall (transcendentals + PDE estimates) left precisely isolated as the frontier — the chapter is the algebra's home, the frontier note the analysis's.* | `theory/math/geometry/riemannian_curvature_tensor.md` |
| 49 | 2026-06-10 | essay | "What IS a character of a finite cyclic group, in 213?" → `theory/essays/synthesis/the_character_is_the_groups_eigen_data.md` | Marathon `/essay` step, after merging main and writing the `curvature_spectrum_crossdomain` note.  The driving question is the **cross-domain synthesis the merge unlocks**: main reads the *multiplicative* quadratic character (`(a/p) = psign σ_a`, the sign of `×a`); this branch's `DiscreteLichnerowicz.km_eigenvalue`/`km_meanzero_eigen` build the *additive* characters as the `K_p` Laplacian `λ=p` eigenspace (spectrum `{0,p}`, mean-zero functions of `ℤ/p`).  The essay's value over either chapter: it states the one object — a character is **eigen-data of the cyclic group acting on itself** — and lists its two faces (multiplication action → sign → Legendre; shift action → eigenbasis → additive characters), both *forced* by cyclicity, neither posited.  Cross-frame: "structure ⟹ spectrum" appears thrice — cyclicity forces the quadratic character (`primitive_not_qr`), `K_p` structure forces the spectrum (`km_eigenvalue`), curvature forces the gap (`lichnerowicz_abstract` `CD(K)⟹λ₁≥K`).  Honest split kept: the `m=p` instantiation is a buildable corollary; the proven morphism identifying `psign σ_a` with an additive-character functional (Gauss-sum-style) is genuinely open.  Pattern: *when a merge brings a branch's spectral object (additive characters as graph-Laplacian eigenspace) into contact with main's number-theoretic object (multiplicative character as permutation sign), the essay is the move that names the single underlying object — the cyclic group's eigen-data — of which both are faces.* | `theory/essays/synthesis/the_character_is_the_groups_eigen_data.md` |
| 50 | 2026-06-10 | promotion | discrete curvature parametric closure (Ollivier `K_m` + Bakry–Émery `K_m`/`K_{1,b}`/`K_{a,b}` + `K_m` Lichnerowicz spectrum) → `theory/math/geometry/discrete_curvature.md` | `/process` step of the post-Ricci-marathon organization branch.  HANDOFF had flagged the discrete-curvature `GeometrizationConjecture/` sub-tree (OllivierRicci 60 / BakryEmery 42 / BakryEmeryBipartite 16 / DiscreteLichnerowicz 11, all PURE re-verified) as a promotion candidate; the four modules form one closed topic — curvature as a parametric stencil readout across graph families (`(m+2)/2`, `(3−b)/2`, `(5−b)/2`, `min(3a−b,b−a+4)/2`) plus its first consequence (Lichnerowicz `CD(K)⟹K≤λ`, `K_m` spectrum `{0¹,m^{m−1}}`) — distinct from both the R1 Geometrization Lens-reading and the smooth tensor machinery (`riemannian_curvature_tensor.md`).  H4 closed by adding the missing STRICT_ZERO_AXIOM entry (the marathon's modules incl. TensorCalculus/IntGridSum had none) + correcting two stale counts (BakryEmery "6 PURE"→42, OllivierRicci unlabeled→60).  Pattern: *a marathon's handoff "promotion candidate" is the next session's first `/process` promotion — and the H4 sweep for it is where stale catalog counts from the marathon surface.* | `theory/math/geometry/discrete_curvature.md` |
| 51 | 2026-06-10 | essay | "What IS the Cauchy–Schwarz inequality, in 213?" → `theory/essays/synthesis/the_cauchy_schwarz_gap_is_a_square.md` | Marathon `/essay` step, after the cross-domain note tying the curvature arc to the proof-ISA arc.  The driving question is a **cross-domain synthesis the promotion surfaced**: A7 POSITIVITY's 2-D Cauchy–Schwarz (`cauchy_schwarz_2d`, gap = one Lagrange square) and the curvature module's n-dim power-mean form (`cauchy_schwarz_gridZ`, per-rung SOS `Σ_j(a_j−a_m)²` folded along the `gridSumZ` induction) are one instruction at two certificate depths.  The essay's value over either module: it lands what the gap *counts* (the total pairwise distinguishing `Σ_{i<j}(a_i−a_j)²`, saturated on the diagonal — `sosGap_eq_zero_of_const` the in-repo witness), names certificate depth as a property of the pointing (the `PresentationDependence` face at the proof layer), and reads the `K_{a,b}` wide/narrow regime split as exactly that depth.  Honest opens kept: the pair-sum identity itself, the two-sequence form, the equality case, the POSITIVITY∘fold compilation claim.  Pattern: *when a promotion's hardest lemma (the narrow-regime Cauchy–Schwarz) turns out to be an existing ISA archetype at a different fold-depth, the essay is the move that names the depth parameter — the archetype catalog holds the instruction, the essay holds what varying its depth means.* | `theory/essays/synthesis/the_cauchy_schwarz_gap_is_a_square.md` |
| 52 | 2026-06-10 | promotion | asynchronous growth arc (point–line system ≅ Raw) → `theory/math/foundations/async_growth.md` | Marathon promotion after the full 8-item frontier agenda closed ∅-axiom (74 PURE): the founding-dialogue system (`seed/ORIGIN_RAW.md` — the axiom rebuilt from "difference" alone, then read as asynchronous growth with no global clock) formalized end to end.  The forced ladder (`step1_forced` / `level2_canonical` exact swap-conjugate disjunction / `level3_diverges` beyond-swap), the depth-2 boundary relocated from run-canonicity (false — no run is forced through the 5-census) to **past-completeness** (an ancestor-order property, `depth3_boundary`: only the full join survives at depth 3), conflict-freeness (`reach_closed` via the new `slash_inj` pair injectivity; `reach_joinable` with no fairness; `every_raw_reached` totality), and the honest census (`honest_count`: soundness + completeness + Nodup — the predicted `Tree.cmp`-transitivity gate dissolved, the strict `Pairwise` invariant needs only the lex head structure) with its structure theorems (quadratic normal form, mod-5 pure 3-cycle as *generic self-restart*, base-2 — not base-5 — sandwich, the two 5s mediated by `choose2 3 = 3`, dagSize event-cost fold with sharing at depth 3).  H1 (74 PURE, 0 DIRTY), H2 (E213 builds), H3 (Theory/Raw + Lib UniverseChain), H4 (STRICT_ZERO_AXIOM 2026-06-10).  S3: frontier note archived; `seed/ORIGIN_RAW.md` landing table now cites the chapter.  Pattern: *an origin dialogue fixed verbatim in `seed/` can serve as the frontier's spec — each of its informal moves (forced stages, lockstep tension, "which scale above level 2") becomes a named theorem, and the chapter's narrative IS the dialogue's answers; also, a multi-agent debate's predicted hard gate (cmp transitivity) should be re-derived before being paid — the right invariant (Pairwise + lex heads) made it free.* | `theory/math/foundations/async_growth.md` |
