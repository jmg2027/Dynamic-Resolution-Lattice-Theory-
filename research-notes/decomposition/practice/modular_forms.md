# Decomposition: modular forms / L-functions

*213-decomposition of "modular forms for `SL(2,ℤ)`, the `q`-expansion `Σ aₙqⁿ`, Hecke operators +
eigenforms, the L-function `L(f,s) = Σ aₙ/nˢ` and its Euler product, the functional equation
`L(s) ↔ L(k−s)`", per `../README.md` (model v7.1). A **fresh** field, LEVERAGE phase — the bar is
PREDICTION/REVELATION. This entry tests whether modular forms **consolidate** four prior decompositions
(`zeta_euler` + `generating_functions` + `fourier`/the character + `spectral`) together with the
repo's substantial **Minkowski modular-symbol / Eichler–Shimura period corpus** — and where the
calculus genuinely BREAKS.*

The four hypotheses under test (from the task):
1. a **modular form** = a reading **invariant under the `SL(2,ℤ)` action** (the modular group = the
   holonomy/`Aut`-family of `curvature.md`/`connections.md`); modular invariance = an `Aut(C)`-invariant
   character (`noether.md`'s `q=+1` invariant). The `q`-expansion `Σ aₙqⁿ` = the generating-function
   family-reading (`generating_functions.md`).
2. the **L-function `L(f,s) = Σ aₙ/nˢ` + its Euler product `Π_p(…)⁻¹`** = `zeta_euler.md`'s `Σ_n = Π_p`
   AGAIN — the L-function is the Dirichlet generating function whose Euler product is the UFD character's
   distributive law (`summatory_mul`). Hecke eigenforms ⟺ the Euler product factorizes (`aₙ`
   multiplicative = the `×↦·` character).
3. **Hecke operators** = the `Aut`-family acting on the space of forms; **eigenforms** = the `q=+1`
   eigenvectors (`spectral.md`'s simultaneous eigenbasis of the commuting Hecke operators); `aₚ` = the
   character readout.
4. the **functional equation `L(s) ↔ L(k−s)`** = a `q=±1` reflection involution (the `s ↔ k−s`
   order-reversing reading, `FenchelMoreau` antitone involution / the φ-Cassini reflection); the central
   value = the reflection's fixed point.

## The decomposition (C / Reading / Residue)

- **Construction `C` — the `SL(2,ℤ)` action on the upper-half-plane lattice, which the repo builds as
  the `Mat2` ×-construction acting on the Stern–Brocot / Farey tree of cusps.** `SL(2,ℤ)` is
  `connections.md`'s holonomy monoid `(Mat2, mul)` restricted to `det = 1`
  (`ModularElliptic.modular_generators_in_SL2`), generated by the two elliptic generators
  `S = [[0,−1],[1,0]]` (order 4, `S²=−I`, the Gaussian `i`) and `U = [[0,−1],[1,1]]` (order 6, `U³=−I`,
  the Eisenstein `ω`) — `PSL(2,ℤ) = ℤ/2 ∗ ℤ/3` (`modular_generator_orders`). This is *exactly*
  `curvature.md`/`connections.md`'s `C`: a `List Mat2` of transitions, read against `I`. The **cusps**
  `{p/q}` it acts on are the Stern–Brocot / Farey rationals (`SternBrocotMarkov`), and a **modular
  symbol** `{p/q, r/s}` is the geodesic between two cusps, with intersection number `symbolDet = qr−ps`
  (`MinkowskiModularSymbol.symbolDet`). Nothing new is constructed — modular forms read this same
  `Aut`-family.

- **Reading `L_mod` — the `SL(2,ℤ)`-invariant family-reading.** A modular form is the reading that is
  **fixed under the whole `Aut`-family** (the `q=+1` Noether-invariant of `noether.md`/`connections.md`,
  `det_holonomy_eq_one`), bundled as a `q`-expansion `Σ aₙqⁿ` — which is *literally*
  `generating_functions.md`'s `L_gen` (the height-indexed count-family, `q` the bookkeeping/grading
  coordinate). Read at the **period** level, `L_mod` is the Eichler–Shimura pairing: integrate the form
  over a geodesic between cusps, i.e. evaluate the form's `q`-expansion against a modular symbol. The
  **L-function** `L(f,s) = Σ aₙ/nˢ` is the *same family `{aₙ}`* read at the **Dirichlet/multiplicative
  index** (`zeta_euler.md`'s `L_ζ` with kernel `n⁻ˢ`), where `generating_functions.md`'s "Cauchy index
  vs Dirichlet index" distinction puts the `q`-expansion and the L-function as two indexings of one
  family. The **Hecke operators** are the `Aut`-family acting *on the space of forms* (the level-up of
  `groups.md`'s composition-closed self-readings); an **eigenform** is the `q=+1` simultaneous
  eigenvector (`spectral.md`), with eigenvalue `aₚ` = the character readout.

- **Residue** — `q = ±1`, surfacing at two strata, exactly as the prior notes locate it.
  - The **functional-equation reflection** `s ↔ k−s` is the order-reversing involution of
    `FenchelMoreau` (`star ∘ star = clo`, antitone self-adjoint) and of `FoldReflections` (the
    det `= −1` reflections `N`, `R` whose product is the elliptic swap `S`, `det = +1`): the `q=±1`
    direction bit, with the central value `s = k/2` the reflection's `q=+1` fixed point.
  - The **analytic L-function / `q`-expansion as a numeric object** (the infinite Dirichlet series, its
    value, analytic continuation, the actual Hecke eigenvalues `aₙ` of a genuine cusp form) is the
    `Real213`-cut RESIDUE — reached by no truncation, the same transcendental-cut residue
    `zeta_euler.md`/`fourier.md`/`generating_functions.md` assign to every infinite series. **And the
    eigenform/Hecke-operator object itself is the genuine ABSENCE** (the `knots.md`-style located break):
    the *space of forms* and the operators acting on it are not constructed.

## Re-seeing — ⟨C | L_mod⟩ ⊕ Residue

```
   SL(2,ℤ) action          =  ⟨ List Mat2, det=1 | L_loop holonomy ⟩          (connections.md's C; S,U generators)
   "modular invariant"     =  the q=+1 Aut-invariant character                (det_holonomy_eq_one; Noether)
   q-expansion  Σ aₙ qⁿ    =  ⟨ {aₙ} height-family | L_gen ⟩, q = grading coord (generating_functions.md)
   L-function  Σ aₙ/nˢ     =  the SAME family at the Dirichlet/× index         (zeta_euler.md's L_ζ)
   Euler product  Π_p(…)⁻¹ =  Σ_n = Π_p, the faithful-valuation distributive law (summatory_mul, geom_sum)
   eigenform aₚ = χ(p)     =  the q=+1 simultaneous Hecke-eigenvector readout   (spectral.md)  [OBJECT ABSENT]
   functional eqn s↔k−s    =  the q=±1 order-reversing reflection involution    (FenchelMoreau star∘star; FoldReflections)
   central value s = k/2   =  the reflection's q=+1 fixed point
   ── the PERIOD shadow, BUILT ──────────────────────────────────────────────
   period {α,β} of f       =  ∫ over the geodesic = a formal homology class      (MinkowskiPeriodIntegral)
   Manin trick             =  every symbol = finite Σ of unimodular {p/q,r/s}, det ±1 (manin_unimodular_decomposition)
                              = the Stern–Brocot / Farey mediant subdivision     (symbolDet_mediant_left/right)
   Eichler–Shimura cocycle =  the ?-defect is a Markov-valued unimodular 1-cocycle on SL(2,ℤ)
                              twist = the SL(2,ℤ) Cayley–Hamilton/Vieta jump      (minkowski_is_markov_valued_cocycle, _twist)
   weight-2 period relation =  the √(−1) congruence m ∣ u²+1 = S's eigenvalue    (minkowski_weight2_period_relation)
   weight-4 period polynomial = 1 − X², the unique line ker(1+S) ∩ ker(1+U+U²)   (period_satisfies_relations)
   E–S period relations    =  r|(1+S)=0 ∧ r|(1+U+U²)=0  from S²=−I, U³=−I        (relations_closed_form)
```

Set side by side, **a modular form is one reading of `connections.md`'s `Aut`-family `C`**, and the
classical machinery splits into a part the repo has heavily built (the **modular-symbol / period /
Eichler–Shimura** side, on the Stern–Brocot tree) and a part it has not (the **form / Hecke / analytic
L-function** side):

| classical object | the calculus's reading | repo status |
|---|---|---|
| modular invariance | the `q=+1` `Aut(C)`-invariant character (`det_holonomy_eq_one`) | the action BUILT; the invariant *form* absent |
| `q`-expansion `Σ aₙqⁿ` | `L_gen` family-reading (`generating_functions.md`) | the GF reading BUILT; the modular `{aₙ}` absent |
| L-function Euler product | `zeta_euler.md`'s `Σ_n = Π_p` | the UFD factorization BUILT; `L(f,s)` object absent |
| Hecke eigenform `aₚ` | `spectral.md`'s `q=+1` simultaneous eigenbasis | **OBJECT ABSENT** (the located break) |
| functional equation `s↔k−s` | the `q=±1` order-reversing involution | the involution engine BUILT; `L`-reflection absent |
| **period / modular symbol** | **the Eichler–Shimura pairing on the Stern–Brocot cusps** | **BUILT, ∅-axiom (the surprise)** |

## LEVERAGE — what falls out, what is built, what BREAKS (leg by leg, honest)

**Net verdict: PREDICTION + PARTIAL.** Three of the four legs are *structurally predicted* and resolve
into prior decompositions; the **Minkowski modular-symbol / period corpus grounds the Eichler–Shimura
side far more than expected** (the Manin trick, the `SL(2,ℤ)` 1-cocycle, the weight-2 and weight-4
period polynomials, period integration — all ∅-axiom PURE); but the **modular-form / Hecke-operator /
analytic-L-function OBJECT is genuinely ABSENT** — a clean located break in the `knots.md` spirit. The
calculus *predicts the shape*; the form itself is the missing leg.

### Leg 1 — modular form = `SL(2,ℤ)`-invariant character. **PREDICTION; action BUILT, form ABSENT (PARTIAL).**
The calculus predicts a modular form is the `q=+1` `Aut(C)`-invariant — the SAME `det_holonomy_eq_one`
Noether/holonomy invariant `connections.md` cites, now read with `SL(2,ℤ)` as the `Aut`-family. The
*action* is built ∅-axiom: `ModularElliptic` constructs `SL(2,ℤ)`'s generators `S` (order 4) and `U`
(order 6), the free-product structure `PSL(2,ℤ) = ℤ/2 ∗ ℤ/3` (`modular_generator_orders`,
`modular_generators_in_SL2`), and `FoldReflections` shows the two det `= −1` reflections (negation,
reciprocal) compose to the det `= +1` elliptic swap `S` (`negation_recip_eq_swap`). The
**invariance/eigenvalue under the generators** is read by the trace: `EllipticTracePeriodic`/
`UTracePeriodic` prove `tr(Sⁿ)` is period-4 and `tr(Uⁿ)` period-6 (`elliptic_orders_four_and_six`) —
the bounded elliptic signature (`disc < 0`), `spectral.md`'s `q=−1` complex-spectrum corner. So the
`Aut`-family the form must be invariant under is fully built and its `q=±1` structure read. **What is
absent**: an actual *form* — a function on `ℍ` (or its `q`-expansion as a Lean object) satisfying
`f(γz) = (cz+d)ᵏ f(z)`. The invariance condition has no operand because the form-space is not
constructed. PARTIAL.

### Leg 2 — L-function Euler product = `zeta_euler`'s `Σ_n = Π_p`. **PREDICTION (consolidation), grounded via `zeta_euler`/`generating_functions`.**
This leg consolidates with no new work: `L(f,s) = Σ aₙ/nˢ` is `zeta_euler.md`'s `L_ζ` reading with the
arithmetic weight `a = {aₙ}` instead of `a ≡ 1`. Its Euler product `Π_p (1 − aₚp⁻ˢ + p^{k−1−2s})⁻¹`
factorizes **iff** `aₙ` is multiplicative (`a_{mn} = aₘaₙ` for coprime `m,n`) — which is *exactly*
`zeta_euler.md`'s faithful-valuation distributive law: `summatory_mul` (multiplicative `f` ⟹ summatory
multiplicative, the finite Euler factorization), `dconv_mul` (Dirichlet convolution preserves
multiplicativity, the L-function ring), `divisor_product_reindex` (the FTA grid reindex), and
`geom_sum`/`sigma_prime_pow_geom` (the local geometric factor). So **"the L-function has an Euler
product" = "`aₙ` is multiplicative" = the `×↦·` character on the family**, and Hecke-eigenform ⟺
multiplicative-`aₙ` is the SAME `Σ_n = Π_p` collapse, now with the per-prime factor a *quadratic*
(`1 − aₚX + p^{k−1}X²`) instead of `zeta_euler`'s `1 − X` — the degree-2 local factor reflecting the
weight `k`. The calculus predicts this exactly. **Honest boundary**: identical to `zeta_euler.md` — the
analytic L-value / infinite product is the `Real213`-cut residue, and the *specific* `aₙ` of a genuine
eigenform are absent (they require the form). The *structure* (Euler product = UFD distributive law) is
the prediction, and it is the SAME ∅-axiom theorem `zeta_euler.md` cites.

### Leg 3 — Hecke eigenforms = the `q=+1` simultaneous eigenbasis. **PREDICTION at the structural level; OBJECT ABSENT (the located BREAK).**
The calculus predicts (via `spectral.md` + `groups.md`): the Hecke operators `Tₙ` are the `Aut`-family
acting on the form-space (a commuting family of linear readings), and an **eigenform** is their `q=+1`
simultaneous eigenvector — the eigenvalue `aₚ` being the character readout, by `spectral.md`'s
"eigenvalue = the `q=+1` scale-residue of the linear reading". This is the predicted shape, and it ties
Leg 2 decisively: the eigenform's `aₚ` *being* the character readout is *why* the Euler product
factorizes (multiplicative `aₙ` = the `×↦·` character = `spectral.md`'s eigenvalue). **But there is NO
Hecke-operator / eigenform / form-space object in the repo** (grep `Hecke`/`eigenform`/`ModularForm`/
`qExpansion` returns only doc-comment mentions in `MinkowskiModularSymbol` and `INDEX.md`). This is the
genuine break — the same shape as `knots.md`'s missing invariant and `spectral.md`'s located break (the
general `d > 1` spectral theorem is absent): the calculus *names the structure precisely* (commuting
operators, simultaneous `q=+1` eigenbasis, eigenvalue = character), but the operators acting on a
form-space are an absent construction. The repo's spectral apparatus is `d = 2` `Mat2` only
(Cayley–Hamilton, the order-2 dial); a Hecke algebra on an infinite-dimensional form-space is not built.

### Leg 4 — functional equation `s ↔ k−s` = the `q=±1` reflection involution. **PREDICTION; involution engine BUILT, `L`-reflection absent (PARTIAL) — but the PERIOD shadow is BUILT.**
The calculus predicts the functional equation is a `q=±1` order-reversing involution with the central
value the `q=+1` fixed point. The **involution engine is built ∅-axiom**: `FenchelMoreau`'s antitone
self-adjoint `star ∘ star = clo` (`biconjugate_eq_clo`, `closed_iff_fixed`: the fixed points are the
closed/`q=+1` locus) is the abstract order-reversing-involution-with-fixed-point shape; `FoldReflections`
gives the concrete det `= −1` reflections whose square is `I` (`N_involutive`, `R_involutive`). **What
is absent**: the actual `L(s) ↔ L(k−s)` (it needs the analytic L-function, leg 2's residue). **The
surprise — the algebraic shadow of the functional equation IS built**: the **Eichler–Shimura period
relations** are precisely the involution-and-cyclic conditions the functional equation's reflection
imposes on the period polynomial. `MinkowskiPeriodPolynomial` proves the weight-4 period polynomial is
`1 − X²`, the unique line annihilated by `r|(1+S) = 0` (the `S²=−I` *involution* condition — the `q=−1`
reflection: `slashS_period_eq_neg` shows `1−X²` is the `−1`-eigenvector of `S`) and `r|(1+U+U²) = 0`
(the `U³=−I` order-3 condition); `period_satisfies_relations`, `relations_closed_form` solve the finite
`ℤ`-linear system. The weight-2 case `minkowski_weight2_period_relation` (`m ∣ u²+1`, the `√(−1)`
congruence = `S`'s eigenvalue, `weight2_period_is_S_eigenvalue`) is the `q=±1` reflection at the lowest
weight. So the `S = [[0,−1],[1,0]]` *involution* (`z ↦ −1/z`, the reflection across the imaginary axis)
**is** the period-side functional equation, ∅-axiom — and `S` being the order-4 det `=+1` swap built
from two det `=−1` folds (`FoldReflections`) realizes "the functional equation = a `q=±1` reflection"
on the modular-symbol corpus. PARTIAL: the period-side reflection is built; the analytic
`L(s) ↔ L(k−s)` is the residue.

## ★ The surprise — what the Minkowski / ModularGeometry corpus actually grounds

The task flagged it (cf. `connections.md`'s `TensorCalculus` discovery), and it holds: the repo's
**Minkowski modular-symbol corpus grounds the entire Eichler–Shimura *period* side of modular forms,
∅-axiom PURE** — far more than "a few matrices". Concretely built:

- **The Manin trick** (`MinkowskiModularSymbol.manin_unimodular_decomposition`): every modular symbol
  `{p/q, r/s}` decomposes into a finite sum of unimodular symbols (`det = ±1`) via mediant subdivision —
  and the mediant *is* the Stern–Brocot/Farey tree. So the period contour's combinatorial skeleton is
  the repo's own Stern–Brocot tree (`symbolDet_mediant_left/right`, `root_symbol_unimodular`).
- **The Eichler–Shimura 1-cocycle on `SL(2,ℤ)`** (`MinkowskiCocycle`): the Minkowski-`?` defect is a
  Markov-valued unimodular 1-cocycle (`minkowski_is_markov_valued_cocycle`), with the cocycle twist =
  the `SL(2,ℤ)` Cayley–Hamilton/Vieta jump (`minkowski_cocycle_twist`), and the defect extends off the
  tree to all of `M₂(ℤ)` (`cocycle_defect_general`, a universal bilinear identity, unimodular exactly on
  `SL(2,ℤ)`).
- **The period polynomials** (`MinkowskiPeriodPolynomial`): the weight-4 period polynomial `1 − X²`, the
  Eichler–Shimura period relations `r|(1+S)=0 ∧ r|(1+U+U²)=0` solved as a finite `ℤ`-linear system; the
  weight-2 period relation as the `√(−1)` congruence = `S`'s eigenvalue (`MinkowskiPeriodRelations`).
- **Period integration** (`MinkowskiPeriodIntegral`, `MinkowskiHigherWeightPeriod`): the weight-2 period
  integral and the higher-weight polynomial integrands `∫ zᵏ dz` are ∅-axiom via the repo's own dyadic
  Riemann/FTC integral (`weight2_period_integral_pure`, `higher_weight_period_integrands_integrate`,
  `period_integrand_modulus_explicit`) — integration is *native*, not Mathlib-dependent.

So the corpus grounds the **modular symbol = the `SL(2,ℤ)`-equivariant period pairing on the
Stern–Brocot cusps**, with the Eichler–Shimura cocycle, the Manin decomposition, the period polynomials,
and the period integrals all built. What it leaves — by the corpus's own honest scope notes — is the
**complex modular contour over `ℍ`** and the **modular-form `f` itself** (the analytic atom: the value
of `f`'s period over a single unimodular symbol). That is the precise missing leg.

## The precise missing leg

The genuine absence is **the modular form / Hecke operator / analytic L-function object** — the
*automorphic* side, as opposed to the *cohomological/period* side the corpus builds:
- no **form-space** / `q`-expansion `Σ aₙqⁿ` as a Lean object (the `Aut`-invariant function on `ℍ`);
- no **Hecke operator** `Tₙ` / **eigenform** / simultaneous-eigenbasis (leg 3's located break);
- no **analytic L-function** `L(f,s)` / its functional equation / the central value (leg 4's residue);
- the **`ℍ`-contour** for the analytic period of a genuine form (the corpus's own stated frontier).

By Eichler–Shimura, the period side (built) and the form side (absent) are dual: `H¹(SL(2,ℤ), V_{k−2}) ≅`
cusp forms ⊕ Eisenstein. The repo has built the **`H¹` / period-cocycle side** and named the **cusp-form
side** as the open frontier (`MinkowskiCocycle`'s "the full `H¹(SL(2,ℤ), V_k)` identification and the
higher-weight period integrals are open"). So the missing leg is precisely *the form realizing the
period* — the same shape as `knots.md` (the invariant absent though the braid group is built) and
`spectral.md` (the `d>1` spectral theorem absent though `d=2` is built).

## Revelation

**Collapse + located break — a modular form is one reading of `connections.md`'s `SL(2,ℤ)` `Aut`-family,
and the classical machinery splits cleanly along the `q=±1` / character / `Aut`-invariant spine into
five prior decompositions PLUS the built period corpus — with the form/Hecke/L-function OBJECT the one
genuine absence.** The decomposition consolidates:
- modular invariance = the `q=+1` `Aut(C)`-invariant character (`noether.md`/`connections.md`,
  `det_holonomy_eq_one`), with `SL(2,ℤ)` = the holonomy `Aut`-family (`S` order 4, `U` order 6, built);
- the `q`-expansion = `generating_functions.md`'s `L_gen` family-reading; the L-function = the SAME
  family at the Dirichlet index, so the **Euler product = `zeta_euler.md`'s `Σ_n = Π_p`** (the UFD
  `×↦·` character, `summatory_mul`) — eigenform-multiplicativity = the character;
- Hecke eigenforms = `spectral.md`'s `q=+1` simultaneous eigenbasis, eigenvalue `aₚ` = the character
  readout (predicted, OBJECT ABSENT);
- the functional equation = the `q=±1` reflection involution (`FenchelMoreau` antitone `star∘star`,
  `FoldReflections`' det `=−1` folds composing to `S`), with the central value its `q=+1` fixed point —
  and its **algebraic shadow, the Eichler–Shimura period relations `r|(1+S)=0`, is BUILT** as the `S`
  involution / `1−X²` period polynomial.

So modular forms are **not a new field** — they are the `SL(2,ℤ)` `Aut`-family read four ways (invariant
/ generating-family / Dirichlet-character / reflection-involution), tying `connections.md` + `noether` +
`generating_functions` + `zeta_euler` + `fourier`/character + `spectral` into one picture. The deepest
revelation is the **surprise grounding**: the repo's Minkowski modular-symbol corpus builds the entire
*period / Eichler–Shimura* side (Manin trick, `SL(2,ℤ)` cocycle, period polynomials `1−X²`, period
integration), ∅-axiom — the cohomological dual of the modular form. The **break** is the *automorphic*
side: the form, the Hecke algebra, the analytic L-function are absent — the precise missing leg, located
exactly where the corpus's own scope notes place the frontier (the `ℍ`-contour + the form realizing the
period). **EXTEND by consolidation + one located break (the form/Hecke/L object); no new axis; model
v7.1 holds.**

## Note for the technique

- **Modular forms are the geometry-cluster + number-theory-cluster meeting point** — `connections.md`'s
  `SL(2,ℤ)` holonomy `Aut`-family meets `zeta_euler.md`'s Dirichlet/Euler family-reading, with
  `spectral.md`'s eigenvalue-as-character the bridge (the Hecke eigenvalue `aₚ` = the `×↦·` character =
  why the Euler product factorizes). The single character arrow now runs through the L-function too.
- **The period side ⊥ the form side (Eichler–Shimura duality), and the repo built the period side.**
  This is a sharp instance of the `connections.md` lesson — grep before declaring a gap: the
  modular-symbol/period/cocycle/period-polynomial corpus grounds far more than "a few `Mat2` lemmas".
  The form/Hecke/L-function break is real, but strictly the *automorphic dual* of a built cohomological
  corpus, not a total absence.
- **The functional-equation reflection has a BUILT algebraic shadow** — the `S`-involution period
  relation `r|(1+S)=0` (`1−X²` the `−1`-eigenvector of `S`). The `q=±1` reflection tag thus reaches the
  Eichler–Shimura period polynomial, not only `FenchelMoreau`/`FoldReflections` — a new grounding of the
  reflection pole.

---

### Verified Lean anchors (file:line — all grep + `tools/scan_axioms.py`-verified this session; all PURE)

| Leg | Theorem (file : name : line) | Status |
|---|---|---|
| `SL(2,ℤ)` = the `Aut`-family: generators `S` (ord 4), `U` (ord 6), `PSL = ℤ/2∗ℤ/3` | `Lib/Math/NumberSystems/Real213/ModularGeometry/ModularElliptic.lean : modular_generator_orders` (:58), `modular_generators_in_SL2` (:66), `S` (:48), `U` (:51), `mul` (:40) | **PURE (scanned 30/0)** ✓ |
| the elliptic trace reads the `Aut`-orders (period 4 / 6, `disc<0` bounded, `q=−1`) | `…/ModularGeometry/EllipticTracePeriodic.lean : elliptic_trace_periodic` (:55), `S_trace_period4` (:43); `…/UTracePeriodic.lean : elliptic_orders_four_and_six` (:61), `U_trace_period6` (:47) | **PURE** ✓ |
| ★ functional-equation reflection (`q=±1`): det `=−1` folds compose to `S` (the involution `z↦−1/z`) | `…/ModularGeometry/FoldReflections.lean : negation_recip_eq_swap` (:55), `recip_negation_eq_neg_swap` (:59), `N_involutive` (:42), `R_involutive` (:43) | **PURE** ✓ |
| functional-equation reflection (abstract): antitone involution `star∘star=clo`, fixed = `q=+1` closed | `Lib/Math/Order/FenchelMoreau.lean : biconjugate_eq_clo` (:59), `cloAntitone_extensive`, `closed_iff_fixed`, `star_triple` | PURE (per `convex_duality.md`) ✓ |
| ★★ Manin trick: every modular symbol = finite Σ unimodular `{p/q,r/s}` (det ±1) via Stern–Brocot mediant | `…/Minkowski/MinkowskiModularSymbol.lean : manin_unimodular_decomposition` (:52), `symbolDet_mediant_left` (:33), `symbolDet` (:28), `root_symbol_unimodular` (:59) | **PURE** ✓ |
| ★★ Eichler–Shimura 1-cocycle on `SL(2,ℤ)`: Markov-valued, unimodular; twist = Cayley–Hamilton/Vieta jump | `…/Minkowski/MinkowskiCocycle.lean : minkowski_is_markov_valued_cocycle` (:46), `minkowski_cocycle_twist` (:91), `cocycle_defect_general` (:115), `minkowski_weight2_period_relation` (:128) | **PURE** ✓ |
| ★★★ weight-4 period polynomial = `1−X²`; E–S period relations `r|(1+S)=0 ∧ r|(1+U+U²)=0` (the reflection-side functional eqn) | `…/Minkowski/MinkowskiPeriodPolynomial.lean : period_satisfies_relations` (:53), `relations_closed_form` (:73), `slashS_period_eq_neg` (:58), `slashU_period_order3` (:62), `period_line_in_kernel` (:89) | **PURE** ✓ |
| weight-2 period relation = `√(−1)` congruence = `S`'s eigenvalue (the `(1+S)` generator) | `…/Minkowski/MinkowskiPeriodRelations.lean : weight2_period_is_S_eigenvalue` (:47) | **PURE** ✓ |
| period integration ∅-axiom (native dyadic Riemann/FTC); higher-weight `∫zᵏdz` + modulus | `…/Minkowski/MinkowskiPeriodIntegral.lean : weight2_period_integral_pure` (:55), `affine_period_depth0_closed` (:88), `period_integrand_modulus_explicit` (:120); `…/MinkowskiHigherWeightPeriod.lean : higher_weight_period_integrands_integrate` (:40) | **PURE** ✓ |
| ★ L-function Euler product = `Σ_n=Π_p` (the UFD `×↦·` character, eigenform-multiplicativity) | `Lib/Math/NumberTheory/SummatoryMultiplicative.lean : summatory_mul`; `DirichletMultiplicative.lean : dconv_mul`; `GeometricSeries.lean : geom_sum`; `DivisorMultiplicative.lean : divisor_product_reindex` | ∅-axiom (per `zeta_euler.md`) ✓ |
| `q`-expansion = `L_gen` family-reading; product = convolution; `q` = grading coord | `Lib/Math/Combinatorics/GeneratingFunction.lean : xVar`, `convolution`; `…/PowerSeriesSemiring.lean : power_series_semiring`; `Probability/Limit/ConvolveProfile.lean : mass_conv` | ∅-axiom (per `generating_functions.md`) ✓ |
| modular invariance = `q=+1` `Aut(C)`-invariant character (holonomy `det=1`) | `…/ModularGeometry/HolonomyLattice.lean : det_holonomy_eq_one`, `holonomy_append` | ∅-axiom (per `connections.md`/`noether.md`) ✓ |
| eigenform `aₚ` = `q=+1` eigenvalue = `×↦·` character readout (`spectral` apparatus, `d=2`) | `…/Mat2/Mat2Spectrum.lean : det_tr_split_is_e1_e2`; `Mat2CayleyHamilton.lean : cayley_hamilton` | ∅-axiom (per `spectral.md`) ✓ |

### Conceptual-only legs / the precise located break (honest — NOT cited as built)

- **The modular form / `q`-expansion `Σ aₙqⁿ` as a Lean object** (the `Aut`-invariant function on `ℍ`,
  the actual eigenvalues `aₙ`) — ABSENT. grep `ModularForm`/`qExpansion`/`cusp form` returns only
  doc-comment mentions (`MinkowskiModularSymbol`, `INDEX.md`). The `Aut`-family acted-upon is built; the
  invariant form is not. The structural prediction (it = the `q=+1` invariant family-reading) is the
  deliverable.
- **The Hecke operator `Tₙ` / eigenform / simultaneous eigenbasis** — ABSENT (grep `Hecke`/`eigenform`
  finds nothing in Lean). The genuine located break, the `knots.md`/`spectral.md`-style boundary: the
  calculus names the structure (commuting operators, `q=+1` simultaneous eigenvector, `aₚ` = character)
  but a Hecke algebra on an infinite-dimensional form-space is not constructed (`spectral.md`'s `d>1`
  break in a new field).
- **The analytic L-function `L(f,s)` / its functional equation `L(s)↔L(k−s)` / the central value** — the
  `Real213`-cut residue (the infinite Dirichlet series as a number), same boundary as `zeta_euler.md`.
  Not claimed; the involution *engine* (`FenchelMoreau`) and the period-side reflection (`r|(1+S)=0`) are
  built, but the `L`-reflection itself needs the analytic L-function.
- **The complex modular contour over `ℍ`** (the analytic atom: `f`'s period over one unimodular symbol)
  — the corpus's own stated frontier (`MinkowskiCocycle`, `MinkowskiHigherWeightPeriod`); the
  combinatorial/period skeleton and polynomial integration are built, the `ℍ`-line-integral of a genuine
  form is not.
