# Decomposition: the fundamental group / algebraic topology

*213-decomposition per `../README.md` (model v7.1). Builds on `curvature.md` (the loop-reading
`L_loop`, holonomy, the `q=±1` deficit), `homology.md` (`∂²=0`, `H₁` = the loop residue),
`groups.md` (a group = the `Aut`-family of `C`-preserving readings, composition forced),
`galois_correspondence.md` (the order-reversing closure + the commutator/derived series at `q=±1`),
and — load-bearing — `knots.md` (the **first located break**: the isotopy quotient, a quotient by an
ambient deformation no reading's kernel generates). **Hypothesis under test:** `π₁(X,x₀) =` the
loop-reading's `q=±1` residue (a loop = a closed path = the holonomy reading; `π₁ =` loops mod
homotopy = the monodromy/holonomy group of the distinguishing); `H₁ = π₁` abelianized = the
commutator quotient (`galois_correspondence.md`'s derived series); covers ↔ subgroups = the Galois
anti-iso. The sharp question: **is `π₁`'s homotopy quotient EXACTLY `knots.md`'s isotopy break — the
same missing primitive — so that `π₁` is a clean PARTIAL-BREAK at the same spot?***

## The decomposition (C / Reading / Residue)

- **Construction `C`** — a **distinguishing-history carrying a loop structure**: the same
  `List Mat2` of state-transitions `curvature.md` reads (`HolonomyLattice.holonomy`), or — at the
  graph level — the 1-complex `K_{m,n}` whose cycle space `homology.md`/`curvature.md` measure. A
  **path** is a `List` of transitions; a **loop based at `x₀`** is a path that returns to its
  basepoint. `C` carries the README's two read-off sub-structures: the **direction bit `q=±1`** (the
  orientation of a crossing/step — over/under, the sign-fold `S`) and the **fold-height** (which is
  *not* what `π₁` reads — `π₁` is degree-1, the loop axis, not the height axis `∂` descends).

- **Reading `L_loop` (the loop-composition / holonomy reading)** — fold a loop to its **net
  transition** `holonomy w = g₀·g₁·…·gₙ·I`, read against the identity `I`. This is `curvature.md`'s
  genuine connection reading: a **monoid homomorphism** from the free monoid of paths
  `(List Mat2, ++)` to `(Mat2, ·)` (`holonomy_append`), so loops compose functorially — exactly the
  concatenation that `π₁` uses for its group operation. `π₁`'s composition `[γ]·[δ] = [γ·δ]` *is*
  `L_loop`'s `holonomy (p ++ q) = holonomy p · holonomy q`, and `groups.md`'s four axioms (identity =
  the constant loop = `holonomy [] = I`; inverse = the reversed loop = the undo-reading
  `composeList_invPerm_left`; associativity = `composeList_assoc`/`holonomy_append`) are *forced* by
  "loops compose". So the **loop *group* part of `π₁` is `groups.md`'s `Aut`-family read around a
  loop** — the holonomy/monodromy group.

- **Residue** — TWO residues, at the two poles of the README's `q=±1` tag, and they split the verdict:
  - **the `q=−1` loop residue (the BUILT part):** the surplus by which `holonomy w ≠ I` — a
    non-contractible loop, born exactly when the sign-fold `S` enters
    (`first_loop_is_the_fold : holonomy [S,S] = −I ≠ I`), while the positive/tree sector is loop-free
    (`positive_loop_trivial`). This is `curvature.md`'s curvature deficit and, via Gauss–Bonnet
    `total = 2 − 2·b₁` (`totalCurv_eq`), `homology.md`'s `H₁` (`b₁` = the cyclomatic loop count).
    "`π₁` trivial = simply connected = `q=+1`, every loop bounds" vs "`π₁ ≠ 1` = non-contractible
    loops = `q=−1` escape residue" is the README's residue tag read at the loop axis.
  - **the homotopy quotient (the ABSENT part — `knots.md`'s SAME break):** `π₁` is loops *modulo
    homotopy* — two loops are identified iff connected through a continuous one-parameter family
    `H : S¹ × [0,1] → X`. That quotient is **not generated by any reading's self-application** and
    **not a kernel-coincidence** (`lensIso_iff_kernel_eq`): it is a quotient by an *ambient continuous
    deformation*, the **exact** structure `knots.md` located as missing (the isotopy quotient in
    `S³`). The calculus has the loop *words* (holonomy products) but not the relation that says two
    different words name the same homotopy class.

## Re-seeing — `⟨C | L⟩ ⊕ Residue`

```
   a loop based at x₀     =  a closed path  =  ⟨ List of transitions (returns to x₀) | L_loop holonomy ⟩
   loop concatenation     =  L_loop functorial      (holonomy_append: holonomy(p++q)=holonomy p · holonomy q)
   the constant loop e     =  the do-nothing reading  (holonomy [] = I)              — π₁ identity
   the reversed loop γ⁻¹   =  the undo-reading         (composeList_invPerm_left)     — π₁ inverse
   π₁ as a GROUP           =  ⟨ loops | the Aut-family of loop-compositions ⟩ = groups.md's Aut-family
                              = the holonomy / monodromy group of the distinguishing
   "simply connected"      =  q=+1: holonomy=I for every loop  (positive_loop_trivial, det_holonomy_eq_one)
   non-contractible loop    =  q=−1 residue: holonomy w ≠ I    (first_loop_is_the_fold: holonomy[S,S]=−I)
   H₁ = π₁ abelianized      =  π₁/[π₁,π₁]  =  killing the q=−1 commutator (galois_correspondence's derived series)
                              =  the cyclomatic b₁ via Gauss–Bonnet  total = 2 − 2·b₁  (totalCurv_eq)
   covers ↔ subgroups of π₁  =  the Galois anti-iso  Fix ⊣ Inv, clo = Inv∘Fix  (order-reversing closure)
        universal cover      =  the trivial subgroup  =  the top of the lattice  (clo-closed, q=+1)

   the homotopy quotient    =  loops mod a CONTINUOUS ambient deformation
                            =  knots.md's ISOTOPY QUOTIENT — the SAME located break, ABSENT
```

Set against the four cross-frames: the **loop-group rows fit perfectly** (they are `curvature.md`'s
holonomy + `groups.md`'s `Aut`-family verbatim), the **abelianization row is `galois_correspondence`'s
derived series** (`H₁ = π₁/[π₁,π₁]` = killing the `q=−1` commutator, one step of the derived series),
the **covers row is `galois.md`'s order-reversing closure**, and the trouble is concentrated in the
**last row** — exactly `knots.md`'s second gap.

## LEVERAGE — does the hypothesis fall out, and is the break `knots.md`'s break?

**Verdict: PARTIAL — three algebraic legs EXTEND (loop-holonomy group, abelianization = commutator
quotient, covers↔subgroups = the Galois anti-iso, all grounded), the FOURTH (the homotopy quotient)
BREAKS at the SAME spot as `knots.md`.** Leg by leg, honestly.

**(1) `π₁` = the loop-holonomy reading's `q=±1` residue, as a GROUP — EXTENDS, grounded.** This is
not asserted; it is `curvature.md`'s holonomy read as `groups.md`'s `Aut`-family:

- The loop concatenation is `holonomy_append` (a monoid homomorphism, ∅-axiom PURE) — `π₁`'s group
  operation IS `L_loop`'s functoriality. The constant loop = `holonomy [] = I`, the inverse loop =
  the undo-reading (`composeList_invPerm_left`), associativity = `composeList_assoc`. So the **group
  structure of `π₁` is forced by "loops compose"**, exactly `groups.md`'s relabel-and-compose.
- The `q=±1` residue: `positive_loop_trivial` (the tree/positive sector is loop-free = `q=+1` =
  simply connected) vs `first_loop_is_the_fold` (`holonomy [S,S] = −I ≠ I` = the first
  non-contractible loop = the `q=−1` deficit, born when the sign-fold `S` enters). "Trivial `π₁` ⟺
  every loop bounds ⟺ `q=+1`" and "`π₁ ≠ 1` ⟺ non-contractible loop ⟺ `q=−1` escape residue" is the
  README's residue tag at the loop axis, and `det_holonomy_eq_one` is the flat (`q=+1`) pole shared
  with `noether.md`/`curvature.md`. So **`π₁` = the holonomy/monodromy group = `L_loop`'s `q=±1`
  residue read as a group** lands cleanly, no new primitive.

**(2) `H₁ = π₁` abelianized = the commutator quotient — EXTENDS, grounded as ONE step of the derived
series.** This is the sharpest *new* tie this entry makes, and it is `galois_correspondence.md`'s
machinery used at degree 1:

- `H₁ = π₁/[π₁,π₁]` is **the first quotient of the derived series** — `galois_correspondence.md`'s
  `DerivedSeries.commSet` (the deduplicated set of group commutators `gcomm a b = a⁻¹b⁻¹ab`, PURE).
  `H₁` = "kill the `q=−1` bracket-residue once": abelianizing IS quotienting by `commSet G`, and the
  commute-test `gcomm_id_iff_commute` (`gcomm a b = e ⟺ ab = ba`) is precisely "the abelianized class
  is trivial". The repo *has* this step concretely: `derived_S3_step1 : commSet S₃ = A₃` is one
  abelianization step on a real permutation group (21/0 PURE).
- The bridge to `homology.md` is `curvature.md`'s Gauss–Bonnet `total = 2 − 2·b₁` (`totalCurv_eq`,
  with `b₁ =` the cyclomatic number `E − V + 1`, `euler_eq_one_sub_b1`): `b₁` counts the *abelianized*
  loops (the cycle space, which is `H₁`), the abelian shadow of the holonomy group. So **`H₁` = the
  abelianized loop residue = one derived-series step on the holonomy group**, tying `homology.md`'s
  `H₁` (`reduced_betti_d4_contractible`: `b̃₁ = 0` on the contractible Δ⁴) directly to the `q=−1`
  commutator. The abelianization *operation* (kill `[π₁,π₁]`) is grounded; the general nonabelian
  `π₁` whose abelianization it is, is not (it is the loop *word* group, present as holonomy products,
  but with no homotopy relation — see leg 4).

**(3) covers ↔ subgroups of `π₁` = the Galois anti-iso — EXTENDS abstractly, grounded as the closure
machine.** This is `galois.md`/`galois_correspondence.md` instantiated at the covering-space lattice:

- The Galois correspondence of covers is the **order-reversing closure** `Fix ⊣ Inv`,
  `clo = Inv∘Fix` (`GaloisConnection.gc_unit`/`gc_counit`/`clo_idempotent`, 15/0 PURE): the lattice of
  connected covers ↔ the subgroup lattice of `π₁`, with the **universal cover ↔ the trivial subgroup**
  (the top of the lattice, the `clo`-closed `q=+1` fixed point). "Bigger cover ⟺ smaller subgroup" is
  the anti-iso, the README's `divides ⟺ refines` dictionary (`ModNat.divides_refines` +
  `refines_implies_divides`) with one order flipped — the *exact* shape `galois_correspondence.md`'s
  `Gal(ℚ(ζ₅)/ℚ)≅C₄` realizes for fields. Covers = resolution-lifts: a cover is a refinement of the
  base (the étale/`sheaf_theory.md` bundle), so the cover lattice is a reading-refinement lattice and
  the correspondence is `galois.md`'s connection on it.
- Grounded only **abstractly**: the closure machine and the anti-iso skeleton are PURE, but — exactly
  as `galois.md` flagged for fields and `galois_correspondence.md` partially closed for `ℚ(ζ₅)` —
  there is **no actual `CoveringSpace`/`π₁`-action/`Inv = fixed-cover` object** in the repo. The
  *structure* the correspondence would inhabit is present and certified; the covering-space *instance*
  is not (it needs the homotopy quotient of leg 4, so it cannot exist without crossing the break).

**(4) the homotopy quotient = `knots.md`'s SAME located break — BREAKS, ABSENT (the central finding).**
`π₁` is loops *modulo homotopy*. The homotopy relation `γ ≃ δ` iff there is a continuous family
`H : S¹ × [0,1] → X` is:

- **not a self-application residue** (no diagonal, no `q=±1` fixed-point asymptote generates it);
- **not a kernel-coincidence** `lensIso_iff_kernel_eq` (it is not "two readings with equal kernel");
- **not the closure `clo`** (it is not idempotent-closure on a refinement lattice);
- it is a **quotient by an ambient continuous deformation** — *precisely* `knots.md`'s Gap 2, the
  isotopy quotient in `S³`. `knots.md` located it as "the un-built colimit/`q=−1` corner PLUS an
  absent ambient-space construction" (v7.1). The homotopy quotient of `π₁` is the **same object**: a
  continuous deformation in an ambient space the calculus has no `Raw`/`Lens` construction for. Grep
  confirms `π₁`, `fundamentalGroup`, `pathHomotopy`, `CoveringSpace`, `vanKampen` are **all absent**
  in `lean/E213` — the break `knots.md` predicted recurs verbatim.

So the loop part EXTENDS (holonomy group, abelianization = commutator quotient, covers = Galois
anti-iso, all grounded — three abstractly, one concretely-shadowed), and the **homotopy quotient is
exactly the isotopy quotient `knots.md` located**. `π₁` is a clean PARTIAL-BREAK at the same spot.

## Revelation

**The recurrence IS the leverage: `π₁`'s homotopy quotient and `knots.md`'s isotopy quotient are ONE
missing primitive, surfacing in two independent fields — which promotes that break from a one-off to a
*principled boundary* of the calculus (the topological-quotient limit), and simultaneously dissolves
three classical theorems into already-built `(C,L)` objects.** This is collapse + residue-surfaced +
the break sharpened, at once:

1. **Collapse — three "different" theorems are three readings of objects the calculus already
   has.** `π₁` (the loop group), `H₁` (its abelianization), and the covers↔subgroups Galois
   correspondence are **not three new structures**: they are (i) `curvature.md`'s holonomy read as
   `groups.md`'s `Aut`-family, (ii) one step of `galois_correspondence.md`'s derived series (kill the
   `q=−1` commutator), and (iii) `galois.md`'s order-reversing closure on the cover-refinement
   lattice. The same `(C, L_loop)` that gives curvature gives `π₁`; the same `commSet`/`gcomm` that
   gives solvability gives `H₁`; the same `clo` that gives the field-Galois FT gives covers↔subgroups.
   `H₁ = π₁/[π₁,π₁]` ties `homology.md`'s `H₁` to `lie_theory.md`'s bracket and
   `galois_correspondence.md`'s derived series in **one** quotient — the deepest new collapse here.

2. **Residue-surfaced — "simply connected" is the `q=+1` pole, "non-contractible" the `q=−1`
   pole.** `π₁` trivial ⟺ every loop bounds ⟺ flat holonomy `det = 1` (`det_holonomy_eq_one`,
   `positive_loop_trivial`) — the **`q=+1` converging corner**, the same corner as φ/Gaussian/closure.
   A non-contractible loop is the **`q=−1` escape residue** (`first_loop_is_the_fold`), the loop the
   tree sector cannot fill — the *same* sign as Cantor/Gödel/curvature's deficit. So "the fundamental
   group" stops being a separate object and becomes **the surplus of the loop-reading, tagged
   `q=±1`** — exactly as "irrational"/"homology"/"curvature" became reading-surpluses in prior batches.

3. **The break sharpened — a recurring boundary, not a one-off.** `knots.md` was "the first clean
   partial-break" (one located missing primitive: the isotopy quotient). `π₁` hits **the identical
   missing primitive from a completely different field** (algebraic topology vs knot theory). A break
   that recurs across independent fields is not an accident of one domain — it is a **structural edge
   of the calculus**: the normal form `⟨C-self-application | reading⟩ ⊕ residue(q=±1)` covers
   *algebraic* quotients (kernel-coincidence `lensIso_iff_kernel_eq`, closure `clo`, the commutator
   `commSet`) but has **no topological-quotient primitive** — no quotient by a continuous ambient
   deformation. `π₁`'s recurrence promotes `knots.md`'s break to the calculus's **principled
   topological-quotient limit**: the precise place where the discrete/combinatorial calculus stops and
   genuine point-set topology (continuous deformation in an ambient space) would have to begin. The
   leverage is that this limit is now **located twice, identically** — it is the calculus's honest
   edge, mapped, not a gap to paper over.

**The precise missing leg (named once, for both `knots.md` and here):** an **ambient-deformation
quotient primitive** — a quotient of the construction by a continuous one-parameter family (homotopy =
isotopy), living at the un-built colimit/`q=−1` corner plus an absent ambient-space construction. With
that one primitive, the homotopy quotient of `π₁` and the isotopy quotient of a knot would both become
expressible; without it, the calculus reaches the **loop word group** (holonomy products), the
**abelianization** (the commutator quotient), and the **covers↔subgroups anti-iso skeleton** — but
stops exactly at the homotopy relation that makes them *topological* invariants rather than algebraic
words. The break is real, smaller and more precisely mapped than a "topology is absent" verdict: the
*algebra* of `π₁` is built; the *one* topological quotient is the single recurring absence.

## Verified Lean anchors (file:line — all grep + `tools/scan_axioms.py`-verified this session)

| Leg | Theorem (file : name : line) | Status |
|---|---|---|
| **`π₁` group op = loop concatenation = functorial holonomy** (the `Aut`-family read around a loop) | `Lib/Math/NumberSystems/Real213/ModularGeometry/HolonomyLattice.lean : holonomy` (def, :93), `holonomy_append` (:108) | ∅-axiom ✓ (26/0 PURE) |
| ★ **simply connected = `q=+1` (loop-free); first non-contractible loop = `q=−1` residue** | `…/HolonomyLattice.lean : det_holonomy_eq_one` (:136), `positive_loop_trivial` (:292), `first_loop_is_the_fold` (:313, `holonomy[S,S]=−I≠I`) | ∅-axiom ✓ (26/0 PURE) |
| **the loop group's identity/inverse/assoc forced by compose** (`groups.md`'s `Aut`-family) | `Lib/Math/Algebra/Linalg213/PermGroup.lean : composeList` (:59), `composeList_assoc` (:93), `composeList_invPerm_left` (:184); `ModArith/Zolotarev.lean : mulPerm_comp` (:106, Cayley), `psign_mulPerm_hom` (:133) | ∅-axiom ✓ |
| ★ **`H₁ = π₁` abelianized = ONE step of the derived series** (kill the `q=−1` commutator) | `Lib/Math/Algebra/Linalg213/DerivedSeries.lean : gcomm` (:51), `gcomm_id_iff_commute` (:58), `commSet` (:124), `derived_S3_step1` (:135, `commSet S₃ = A₃`) | ∅-axiom ✓ (21/0 PURE) |
| the `q=−1` commutator = antisymmetry residue (the bracket abelianization kills) | `Lib/Math/NumberSystems/Real213/Mat2/Mat2Bracket.lean : bracket` (:66), `bracket_antisymm` (:76), `tr_bracket_zero` (:101) | ∅-axiom ✓ (10/0 PURE) |
| ★ **`H₁` = the cyclomatic loop count `b₁` via Gauss–Bonnet** (`homology.md`'s residue, abelian) | `Lib/Math/Geometry/DiscreteCurvature/DiscreteGaussBonnet.lean : cyclomatic` (:39, `b₁=E−V+1`), `gauss_bonnet_Kmn` (:42), `euler_eq_one_sub_b1` (:47), `totalCurv_eq` (:53, `total = 2 − 2·b₁`) | ∅-axiom ✓ |
| `H₁` as the boundary-reading residue (`b̃₁=0` on contractible Δ⁴) | `Lib/Math/Cohomology/Examples/BettiKernel.lean : kerSizeDelta` (:42), `kerSize_5_1` (:52), `reduced_betti_d4_contractible` (:63) | ∅-axiom ✓ |
| **covers ↔ subgroups of `π₁` = the order-reversing closure** (`Fix ⊣ Inv`, universal cover ↔ trivial subgroup) | `Lib/Math/Order/GaloisConnection.lean : gc_unit` (:41), `gc_counit` (:49), `gc_fgf` (:79), `gc_gfg` (:91), `clo` (:104), `clo_idempotent` (:126), `gc_unique_right` (:140) | ∅-axiom ✓ (15/0 PURE) |
| the cover-lattice anti-iso skeleton (`divides ⟺ refines`, the resolution-refinement order) | `Lens/Instances/Leaves/ModNat.lean : divides_refines` (:57), `refines_implies_divides` (:75) | ∅-axiom ✓ |
| cross-frame to the `q=±1` residue tag (simply-connected = converge / non-contractible = escape) | `Lib/Math/Foundations/ResidueTag.lean : ResidueTag` (escape\|converge), `multiplier` (∓1) — per `curvature.md`/`homology.md` | ∅-axiom ✓ |

## Conceptual-only legs / the located break (honest — NOT grounded in repo Lean)

- **An actual `π₁(X,x₀)` / path-homotopy / `CoveringSpace` / Seifert–van Kampen object** — **absent.**
  Grep across `lean/E213`: zero `fundamentalGroup`/`fundamental_group`/`π₁`/`pathHomotopy`/
  `PathHomotopy`/`CoveringSpace`/`coveringSpace`/`vanKampen`/`seifert`/`abelianization`. The repo has
  the **loop word group** (holonomy products `holonomy_append`), the **abelianization operation**
  (`commSet`/`gcomm`, one derived-series step), and the **closure machine** (covers↔subgroups
  skeleton) — but **no homotopy quotient** to turn loop words into homotopy classes, hence no `π₁`
  *object*, no `Inv = fixed-cover` instance of the Galois connection on covers.
- **★ The homotopy quotient = `knots.md`'s isotopy quotient — the SAME located break.** `π₁` is loops
  *mod homotopy*; that quotient by a continuous ambient deformation is **not** a self-application
  residue, **not** a kernel-coincidence (`lensIso_iff_kernel_eq`), **not** the closure `clo` — it is
  `knots.md`'s Gap 2 (v7.1: "the un-built colimit/`q=−1` corner + an absent ambient-space
  construction"), recurring verbatim. This is the central honest finding: the break is **not a one-off
  of knot theory** — it is a principled topological-quotient limit, now located twice in independent
  fields. The named missing leg (one primitive for both): an **ambient-deformation quotient** of the
  construction.
- **The covers↔subgroups correspondence as a concrete instance** — abstract-only (exactly as
  `galois.md` flagged for fields before `galois_correspondence.md` found `ℚ(ζ₅)`). The closure
  machine and anti-iso skeleton are PURE; no covering-space instance exists, and it *cannot* without
  the homotopy quotient above. So leg (3) is grounded as the *shape*, not as a worked covering.
- **The general nonabelian `π₁` whose abelianization is `H₁`** — only the abelianization *step* is
  built (`commSet S₃ = A₃`, on a permutation group, not on a loop group with a homotopy relation). The
  derived-series machinery is the operation; the loop group it would act on is the loop-word group
  without the homotopy quotient.

## Verdict: PARTIAL — the loop algebra EXTENDS, the homotopy quotient is `knots.md`'s SAME break

`π₁` **EXTENDS on the algebraic side** with no new primitive: (1) `π₁` as a **group** = the
loop-holonomy reading's `q=±1` residue read as `groups.md`'s `Aut`-family (holonomy `holonomy_append`,
`first_loop_is_the_fold` the `q=−1` first loop, `positive_loop_trivial` the `q=+1` simply-connected
pole — **built, 26/0 PURE**); (2) **`H₁ = π₁` abelianized = the commutator quotient** = one step of
`galois_correspondence.md`'s derived series (`commSet S₃ = A₃`, `gcomm_id_iff_commute`, **built 21/0
PURE**), tied to `homology.md`'s `b₁` via Gauss–Bonnet `total = 2 − 2·b₁` (`totalCurv_eq`); (3)
**covers ↔ subgroups = the Galois anti-iso** = `galois.md`'s order-reversing closure (`clo_idempotent`,
`gc_unit`/`gc_counit`, **built abstractly 15/0 PURE**, universal cover ↔ trivial subgroup = the
`clo`-closed `q=+1` top). It **BREAKS at the homotopy quotient** — loops mod a continuous ambient
deformation — which is **exactly `knots.md`'s isotopy/ambient-deformation primitive**, still absent.
That recurrence is the leverage: the break is no longer a one-off but a **principled topological-
quotient limit**, located twice in independent fields, with **one named missing leg** (an
ambient-deformation quotient at the un-built colimit/`q=−1` corner). **The calculus reaches the loop
word group, the abelianization, and the covers anti-iso skeleton; it stops precisely at the homotopy
relation — `knots.md`'s break, confirmed as a genuine recurring boundary.**
